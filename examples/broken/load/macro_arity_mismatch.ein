;; An invocation's arg count must match the declaration's params. The
;; only fixture here whose message carries a real `Loc` — the error is
;; raised inside macro expansion, on a nested (located) node.

(macro m (?a ?b) (rel ?a ?b))
(rule x () :match (m ?p) :assert (y ?p) :why "w")

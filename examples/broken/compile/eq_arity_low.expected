`(eq …)` takes exactly 2 arguments and was given 1: SForm(head=Atom(name='eq'), args=(Var(name='x'),)) at Loc(file='{FILE}', line=19, col=24) — a built-in predicate compares exactly two values, so write one guard per pair.

# S1.7a.1 — Investigations: finish the analysis

**Phase:** P1.7a. **Estimate:** ½–1 day (read + measure). **Status:** planned.
**Depends on:** the [analysis](analysis.md) open questions (OQ1–4).
**Charter:** close the four questions `analysis.md` cannot answer by reading,
and append the answers back into `analysis.md` as a "Findings" section before
any code lands in S1.7a.2+.

Measurement uses a **single committed script**
(`ein.py/demo/compare_solution_nodes.py` or extend `demo/compare_engines.py`),
not inline one-liners (project convention). Run under PyPy where wall-clock
matters.

## Tasks

- **T1 — open vs all-refuted dead-end (OQ1).** On `zebra2.ein`, capture the
  incomplete grid[0] branch kb (the `color-loc Green House-4` dead-end). For
  its unresolved cells, classify each generable hypothesis as present /
  refuted / open. Confirm `_compute_alive(grid0_kb)` is **non-empty**
  (⇒ excluded as incomplete) and that it is **not** an all-refuted totality
  dead-end (which would have to surface as a contradiction). Record the
  verdict-relevant conclusion.
- **T2 — exhaustive search on all three variants (OQ2).** For
  `zebra2.ein`, `zebra2-minus-15.ein`, `ein-bugs/zebra2-bad.ein`: run the
  exhaustive lattice search; record commitments visited, wall-clock (PyPy +
  CPython), raw solution records, **distinct `state_hash` among
  consistent∧complete nodes (`k`)**, and (for minus-15) the residual open set.
  Targets to confirm: zebra2 `k=1`; minus-15 `k>1` and **terminates** within a
  sane `max_set_size`; bad `k=0` with a 2–3 edge core. If minus-15 does not
  terminate, record the `max_set_size` needed and the `¬exhausted` fallback.
- **T3 — exhaustiveness-certification rule (OQ3).** Define `exhausted`
  operationally: the precise condition under which the loop has reached
  complete-or-dead on every reachable branch within `max_set_size`. Determine
  empirically whether zebra2 certifies **Unique** *without* hitting the depth
  cap (forced-positive collapse should make the residual search shrink to one
  state quickly).
- **T4 — inventory the S1.7.3 experiment (OQ4).** `grep` for
  `solve_requires_complete_model`, `trace/answer.py`, `goal_bindings`,
  `demo/compare_engines.py`, and the monotonic accept-point edits. List
  exactly what exists, so S1.7a.5's revert is surgical (keep the
  harmless/correct pieces per S1.7.3a T4).

## Acceptance

- `analysis.md` gains a dated "Findings (S1.7a.1)" section answering OQ1–4
  with measured numbers and the certification rule.
- The measurement script is committed and `--help`-bearing.
- No engine code changed in this stage (read + measure only).

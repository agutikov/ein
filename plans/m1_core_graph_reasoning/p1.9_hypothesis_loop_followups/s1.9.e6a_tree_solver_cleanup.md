; S1.9.E6a — Remove obsolete tree-solver code (back_prop.py dead
; residue). HARD PREREQUISITE of S1.9.E6: the transitive premise
; walk's live home is commitment._is_unconditional + provenance.reaches,
; NOT back_prop.is_unconditional_death (dead). Clean up before E6.

# S1.9.E6a — Tree-solver dead-code removal (prerequisite for E6)

**Phase:** P1.9.
**Executes immediately before:** [S1.9.E6](s1.9.e6_transitive_premise_walk.md).
**Estimate:** S–M (½–1 day).
**Value:** M — codebase honesty; unblocks an honest E6.
**Status:** 📌 ready — verified-dead inventory below; execute before any E6 work.
**Blocks:** [S1.9.E6](s1.9.e6_transitive_premise_walk.md).

## Why this is a stage, not a followup

The post-S1.5b tree-solver removal (commit `8d77b02`) left `back_prop.py` — the
parent-KB / sibling / ancestor-chain back-prop machinery — as **dead/inert
residue** in the tree. This is *not* a parked "someday" kernel-minimisation
audit item: it actively **misleads** (the [S1.5.7](../p1.5_hypothesis_loop/s1.5.7_back_prop_unconditional.md)
doc and the E6 stub describe its `(not h)`-to-parent mechanism as live; it is
not, and there is no parent KB), and E6 cannot be planned honestly while a **dead
`is_unconditional_death`** sits next to the **live** walk
(`commitment._is_unconditional` + `provenance.reaches`). So: a small, immediate
refactor stage, sequenced before E6 — not an entry in the F5 audit catalogue.

The production engine is the commitment-set cardinality lattice
(`inference/monotonic/`): one root KB, commitment sets related by subset, **flat
root-writes + Apriori nogoods** (`monotonic/__init__.py`: *"no per-parent bubble;
saturation commutativity makes the bubble mechanically redundant"*). No parent
KBs, no sibling inheritance, no ancestor chain — the exact concepts `back_prop.py`
implements.

## Verified inventory (callers audited 2026-06-15)

| symbol(s) | status | evidence |
|-----------|--------|----------|
| `is_unconditional_death` | **dead** | 0 callers repo-wide, 0 tests (it is the E6 predicate) |
| `back_propagate` + helpers (`_bubble_to_ancestors`, `_clear_ancestor_verdict_caches`, `_write_negation`, `_mirror_forced_positive`, `_consume_caches_ctx`) | **inert** | `back_propagate`'s sole call (`hypgen.py:223`) sits behind a lookahead the monotonic `solver.py` never passes; the ancestor bubble reads `_kb_chain_ctx`, **never `.set()`** anywhere (the tree solver's `_explore` was its only setter, gone in `8d77b02`) |
| `BubbleAbort`, `_eager_pass_ctx`, `_bump_pass_bubbled` | **inert (eager mode)** | imported by the live `nogoods.emit_nogood`, but only inside its `if _eager_pass_ctx is not None` block — and `_eager_pass_ctx` is never `.set()`, so the block never runs |

Where the functionality went: the transitive walk → `provenance.reaches()` +
`commitment._is_unconditional`; the negation write → `solver.py`'s own
`_write_negation` (its docstring: *"mirrors `back_prop._write_negation` without
the ContextVar"*); back-prop pruning → Apriori nogoods (`nogoods.emit_nogood` +
`apriori.filter_candidate`).

## Tasks

### Task T1.9.E6a.1 — Delete the fully-dead symbols

`is_unconditional_death` + the ancestor-bubble cluster (`back_propagate`,
`_bubble_to_ancestors`, `_clear_ancestor_verdict_caches`, `_consume_caches_ctx`,
`_mirror_forced_positive`, `_write_negation`, `_kb_chain_ctx`). Re-verify 0
callers at deletion time with an **AST `ImportFrom` audit, not a line-grep**
(multi-line `from … import (…)` blocks false-negative on grep).

### Task T1.9.E6a.2 — Resolve the eager-mode coupling (intent gate)

`nogoods.emit_nogood` (**live**) imports `BubbleAbort` / `_eager_pass_ctx` /
`_bump_pass_bubbled` for an eager block that never runs (`_eager_pass_ctx` is
never `.set()`). **Decide:** is eager-root-bubble (S1.5a.17) abandoned? If yes →
inline a plain insert into `emit_nogood`, drop the eager block + the back_prop
imports. If parked-not-dead → keep, but document why it is retained inert and
what would re-arm `_eager_pass_ctx`.

### Task T1.9.E6a.3 — Remove the module + dangling references

With .1 + .2 done, `back_prop.py` has no live symbols → delete it. Clean the
danglers: the `hypgen.py:223` `back_propagate` call + its import (the unreached
lookahead-cache path — drop the `enable_back_prop_unconditional` branch); the now
orphaned `enable_back_prop_unconditional` SolverConfig flag + its docs; the
`_mirror_forced_positive` comment marker (`store.py:151`); the `BubbleAbort`
mentions in `config.py`; the `back_propagate` `--help` string in
`demo/bench_monotonic.py`.

### Task T1.9.E6a.4 — Update stale tree-solver-vocabulary docs

[S1.5.7](../p1.5_hypothesis_loop/s1.5.7_back_prop_unconditional.md) (mark its
parent-KB back-prop historical / superseded by the lattice); the E6 stub Context
+ Tasks (rewrite in lattice terms — already flagged in its 2026-06-15 note); any
kernel docs that cite `back_prop`.

### Task T1.9.E6a.5 — Validate behaviour-neutral

Full suite green; zebra2 solve/gaps verdict **byte-identical** (`EIN_RUN_SLOW=1`
or the PyPy bench — the deleted symbols have no tests, so removal must be
behaviour-neutral by construction).

## Acceptance

- `grep -rn 'is_unconditional_death\|back_propagate\|_kb_chain_ctx' ein.py/src` → 0.
- `nogoods.emit_nogood` self-contained (no `back_prop` import), **or** the eager
  trio documented-retained per T.2's decision.
- `back_prop.py` deleted (or reduced to only genuinely-live symbols, if T.2 keeps
  the eager machinery there).
- Full suite green; zebra2 verdict unchanged under the slow/PyPy path.
- S1.5.7 + the E6 stub no longer describe the dead parent-KB back-prop as live.

## Cross-links

- [S1.9.E6](s1.9.e6_transitive_premise_walk.md) — the stage this unblocks.
- [F5 — kernel-minimisation audit](../../followups/f5_rules_as_data.md) — records
  the "Back-prop candidate = dead, removed here" audit conclusion (the work lives
  in *this* stage, not there).
- Precedent: P1.7b S1.7b.1 dead-code sweep (commit `0e124ea`); tree-solver
  removal (commit `8d77b02`).

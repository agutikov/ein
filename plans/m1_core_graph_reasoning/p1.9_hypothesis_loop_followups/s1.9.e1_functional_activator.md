; S1.9.E1 — (functional R) activator. Weaker than
; single-parent; declares R is a function on its slot-0.

# S1.9.E1 — `(functional R)` activator

**Phase:** P1.9 (hypothesis-loop follow-ups), §closure
refinements.
**Estimate:** S (≤ ½ day).
**Value:** M.
**Status:** 📅 parked — promotes when puzzles want a weaker
single-parent.
**Catalog row:** [P1.9 README — Closure refinements](README.md#closure-refinements-q-s154b-descendants)
E1.

## Context

Per the catalog row: declare R as a function on its slot-0
(≤ 1 image per slot-0 value). Weaker than `single-parent` /
`functional` (which has been ranamed) — used as an activator
for rules that exploit functional-ness without requiring
totality.

## Tasks

### Task T1.9.E1.1 — Define the activator

- **a.** `(functional R)` shipped as a property fact (it
  already exists in M1; this catalog entry tracks
  *generalisation* beyond the current uses).
- **b.** Document the difference from `single-parent` /
  `functional` (which exists).

### Task T1.9.E1.2 — Use sites

Identify rules that should consume `(functional R)` —
candidates: `domain-elimination` (one-survivor logic),
constraint propagation, conflict-driven learning (E7).

### Task T1.9.E1.3 — Tests

Activator fires; consumer rule narrows correctly; no false
positives on relations marked functional.

## Acceptance

- Activator + consumers shipped or documented as expected
  consumers (depending on demand).

## References

DL `funcProp`; M1's existing `(functional R)` declaration.

## Cross-links

- [P1.9 README — E1](README.md#closure-refinements-q-s154b-descendants)
  — catalog row.
- [S1.8.A6 closure auto-inference](../p1.8_ein_lang_modules/s1.8.a6_closure_auto_inference.md)
  — composes (`functional ∧ total ⇒ closed`).

; S1.9.E11 — Goal-driven hypothesis filter. Only emit h that
; could unify with the query goal (or a sub-goal via rule
; unification); backward chaining trim.

# S1.9.E11 — Goal-driven hypothesis filter

**Phase:** P1.9, §search heuristics.
**Estimate:** M (1-3 days).
**Value:** M.
**Status:** 📅 parked.
**Catalog row:** [P1.9 README — Search heuristics](README.md#search-heuristics)
E11.

## Context

Backward chaining (Prolog SLD-resolution flavour). Hypgen emits
only candidates that could unify with the query goal — directly
or transitively via rule unification.

## Tasks

### Task T1.9.E11.1 — Goal unification index

For each query goal, precompute the set of `(rel, slot)` that
could feed it via rules.

### Task T1.9.E11.2 — Filter integration

A new filter pass in T1.5.4.7's pipeline: drop candidates
outside the goal-reachable set.

### Task T1.9.E11.3 — Tests + bench

A puzzle with a tight query goal; ensure node count drops
without missing solutions.

## Acceptance

- Filter ships behind flag.
- Bench shows node-count reduction on goal-tight puzzles.

## References

Prolog SLD-resolution
([docs/index/03 theorem-proving](../../../docs/index/03-theorem-proving-formal-methods.md)).

## Cross-links

- [P1.9 README — E11](README.md#search-heuristics).
- [F7 §C rule-set sufficiency](../../followups/f7_rule_induction.md)
  — sibling; both aim at "only do what the query needs".

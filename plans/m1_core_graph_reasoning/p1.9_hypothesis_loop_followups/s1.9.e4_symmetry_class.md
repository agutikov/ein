; S1.9.E4 — (symmetry-class R T) activator. Declare T-instances
; interchangeable under R; collapse to a canonical representative
; at gen-time. (The "A5 emit-both hack" it once replaced was
; itself removed by S1.7.24 — symmetry is now pure user space.)

# S1.9.E4 — `(symmetry-class R T)` activator

**Phase:** P1.9, §closure refinements.
**Estimate:** M (1-3 days).
**Value:** M.
**Status:** 📅 parked — **space-shrinker + uniqueness-up-to-symmetry semantic
(2026-06-15)**; the sound form is L-effort (lex-leader SBP / SBDD) and no
current puzzle exercises it. See [§ Lattice re-grounding](#lattice-re-grounding-2026-06-15).
**Catalog row:** [P1.9 README — Closure refinements](README.md#closure-refinements-q-s154b-descendants)
E4.

## Lattice re-grounding (2026-06-15)

Value-symmetry breaking is a **space-shrinker**, but note what it does and does
*not* compose with. `canon.state_hash` collapses **path-duplicate** models
(distinct proof paths reaching the *identical* fact set —
`architecture_and_algorithms.md` §O9); it does **not** collapse value-symmetric
models, because two symmetric solutions have *different* fact sets (`(R a v1)`
vs `(R a v2)`) → different hashes. O9 says as much: full symmetry breaking
"would only matter for far larger or highly-symmetric instances." So **today
symmetric solutions are not pruned at all** — each is explored (a full
saturation per symmetric commitment) and surfaces as a *separate* solution
node, so `solve()` reports **ambiguity** for what is one solution up to
symmetry.

E4 would therefore do two genuinely-new things: (a) **prune** symmetric
branches at gen-time → fewer enterings/saturations (the O1+O2 bottleneck); and
(b) **change the verdict to uniqueness-up-to-symmetry** (collapse the N
isomorphic solution nodes to one) — a *semantic* change a puzzle must opt into.
**Soundness is subtle**: a static "emit one representative" is unsound once
facts break the symmetry mid-search, so a correct E4 needs lex-leader
symmetry-breaking constraints (static SBP) or dynamic SBDD — an L-effort
feature. No current puzzle exercises it (zebra2's houses are
**position-distinguished**, not a symmetry class), so it also needs a synthetic
symmetric fixture. Cold on zebra2; activation awaits a genuinely-symmetric
puzzle.

## Context

CSP-style **value-symmetry breaking**. Declare that T-instances
are interchangeable under R (e.g., the 5 Houses are
indistinguishable until labelled by attributes — *if* the
puzzle's constraints don't distinguish them by position).

At hypgen time: emit one canonical representative per
equivalence class instead of all members.

**Note (2026-06-03).** This entry's original justification — "replaces the A5
emit-both hack" — is stale:
[S1.7.24](../p1.7_bootstrapping_zebra/s1.7.24_dehardcode_symmetric.md)
**removed the engine's symmetric machinery entirely** (`back_prop.py` — no
symmetric mirror; the kernel keys on `is_symmetric` nowhere). Symmetry is now
purely the user's `(rule symmetric)`. So E4 is no longer "replace an engine
hack" but an **optional value-symmetry-breaking optimisation** layered over a
user-declared symmetric relation — a search-space win, not a correctness
crutch.

## Tasks

### Task T1.9.E4.1 — Surface

`(symmetry-class R T)` as a property fact. Engine collapses
hypotheses involving T-instances to a canonical representative
when R is the relation.

### Task T1.9.E4.2 — Canonical representative selection

Lexicographic first? User-declared? Open question.

### Task T1.9.E4.3 — Tests + bench

Smoke: a small symmetric puzzle solves with N-times fewer
branches.

## Acceptance

- Activator works on smoke puzzle.
- On demos with a user `(rule symmetric)`, the canonical-representative
  collapse cuts branches without changing the verdict.

## References

CSP value-symmetry breaking
([docs/index/02 solvers / CSP / SAT / SMT](../../../docs/index/02-solvers-csp-sat-smt.md)).

## Cross-links

- [P1.9 README — E4](README.md#closure-refinements-q-s154b-descendants).
- [S1.5.4 A5 symmetric duplicate emit](../p1.5_hypothesis_loop/s1.5.4_hypgen_improvements.md#already-shipped-engine-has-them-today)
  — the emit-both hack this once targeted; **removed by
  [S1.7.24](../p1.7_bootstrapping_zebra/s1.7.24_dehardcode_symmetric.md)**, so
  there is no longer an engine hack to replace.

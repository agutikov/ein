; S1.9.E7 — Learned-clause from unsat-core. The conjunction of
; source facts that produced this contradiction is itself a
; "learned constraint"; future hypotheses test against it
; without re-running rules.

# S1.9.E7 — Learned-clause from unsat-core

**Phase:** P1.9, §conflict-driven learning.
**Estimate:** L (> 3 days).
**Value:** H (long-term).
**Status:** ✅ **largely resolved 2026-06-15** — the *deriving* half ships (the
source + hypothesis frontier is in `DeadCommitment.unsat_core`, now built on
E6's `walk_premises`); the *pruning* half (CDCL clause-shrinking) is **measured
vacuous** in this engine and closed. Only *minimisation*
([E19](s1.9.e19_unsat_core_min.md)) remains, as its own entry. See
[§ Resolved](#resolved-2026-06-15).
**Catalog row:** [P1.9 README — Conflict-driven learning](README.md#conflict-driven-learning-satcdcl-inspired)
E7.

## Resolved (2026-06-15)

Two findings close E7's substance; only [E19](s1.9.e19_unsat_core_min.md)
(minimise the frontier for trace readability) stays open, as its own entry.

**1. The deriving half already ships — now on E6.** "The conjunction of source
facts that produced this contradiction" is exactly `store.unsat_core`'s output,
stored per-dead in `DeadCommitment.unsat_core`. `DerivationDAG.sources`
(`provenance.py`) — and so `unsat_core` — already includes **both** `source`-kind
givens **and** `hypothesis`-kind committed guesses, so the "learned clause from
the unsat-core" (given frontier + active-hypothesis core) is computed today. As
of E6 it is built on the shared `walk_premises` collector.

**2. The pruning half is structurally vacuous here (measured).** E7's CDCL ask —
"future hypotheses test against [the clause] without re-running rules" — is
*nogood generalisation*: learn a clause **smaller** than the committed set so it
prunes more. Measured on `examples/zebra2.ein` via `contradictions_solve`:
**all 49 dead commitments are size-1 singletons** (`size≥2: 0`); the
active-hypothesis core never shrinks below the commitment. This is structural,
not incidental — the search is a **cardinality BFS + Apriori downward-closure +
forced-positive cascade**: a size-k set is entered only if no proper subset is
already a nogood, and the cascade resolves forced singletons first, so every
emitted nogood is **already minimal**. There is no accumulated
irrelevant-decision tail to strip (that is a DPLL *path-condition* problem; the
set-lattice doesn't have one). Clause-shrinking has nothing to prune.

**3. …and it would not be sound to ship anyway.** Even where a smaller core
existed, generalising the nogood (pruning supersets of it) is unsound under NAF:
a superset commitment can derive a fact that flips an `(absent P)` premise the
core's ⊥ relied on, un-deadening it. A monotone-only guard would need the
⊥-derivation's absent-premise usage, which `Provenance` does **not** record
(`premises_raw` is positive premises only), and zebra2's `domain-elimination`
uses NAF — so the guard could not greenlight it regardless.

**Conclusion.** Deriving half ships (now on E6); the pruning half is closed as
inapplicable (vacuous + unsound-under-NAF + no measurable benefit — search is
not the bottleneck, [E23](s1.9.e23_prove_speedup.md)). The live descendant of
"learned clause from unsat-core" is **[E19](s1.9.e19_unsat_core_min.md)** —
minimise the already-recorded frontier for human-readable contradiction
explanations: a trace-quality, not a search, win.

## What already ships (2026-06-15)

This entry reads as fully greenfield, but the CDCL-flavoured core landed with
the set-search engine (`S1.5b.6`) and is live:

- **Multi-literal learned clause** — `Clause = frozenset[FactId]`
  (`nogoods.py:52`), stored in `KnowledgeBase._nogoods` (`store.py:166`), kept
  subsumption-minimal on insert. Exactly T1.9.E7.1's "set of literals that
  can't all hold," and **separate from the single-fact `_negated_facts` index**
  (`store.py:203`) — also as T1.9.E7.1 specified.
- **Generation on death** — `emit_nogood(root_kb, frozenset(c), min_size=1)`
  at the dead-commitment handler (`solver.py:805`).
- **Consumption pre-fork** — candidate sets are filtered against `_nogoods`
  *before* any fork/saturate via `apriori.filter_candidate`'s subset test
  (`apriori.py:90`, called from `generate_layer`, `solver.py:1050`) — the
  downward-closure principle. Realises T1.9.E7.3's intent, but at the
  **assembled-set** level, not the per-prospective-hypothesis level the task
  described.

**The genuine residual** is narrow:
- **a.** today's clause is the branch's **path condition** (the committed
  set), *not* a clause derived from the **unsat-core** — E7's actual ask
  (`DeadCommitment.learned_clause == frozenset(commitment)`, `contract.py:102`;
  write-only / diagnostic, never read back to prune).
- **b.** it is not **minimised** ([E19](s1.9.e19_unsat_core_min.md)).
- **c.** it does not persist across calls ([E20](s1.9.e20_conflict_cache.md)).

So E7 is best **re-scoped to "MUS-derived, minimised nogoods"** (folding in
[E6](s1.9.e6_transitive_premise_walk.md)'s premise walk + E19's minimiser), not
"learned clauses from scratch."

> **Doc note:** `nogoods.py`'s module docstring advertises `matches_any_nogood`
> + `build_clause` as the consumption path — both are **dead code** (defined,
> never called); the live path is `apriori.filter_candidate`. Worth fixing when
> this entry is touched.

## Context

CDCL-inspired ([docs/lib/02 solvers / CSP / SAT / SMT](../../../docs/lib/02-solvers-csp-sat-smt.md)) +
ATMS justification cache
([docs/lib/09 cognitive architectures / neurosymbolic](../../../docs/lib/09-cognitive-architectures-neurosymbolic.md)).

When a contradiction fires, the unsat-core (a conjunction of
source facts) is itself a "learned constraint". Future
hypotheses can be tested against it directly, without
re-running the rules that produced the contradiction.

## Tasks

### Task T1.9.E7.1 — Learned-clause representation

A learned clause is a set of `(rel, args)` literals that
*can't all hold simultaneously*. Stored alongside hypgen's
`_negated_facts` index — separate index because the clause is
multi-literal, not a single negation.

### Task T1.9.E7.2 — Generation

After a contradiction fires:

- **a.** Walk `Provenance` (using [E6](s1.9.e6_transitive_premise_walk.md))
  to find the unsat-core.
- **b.** Minimise it (via E19).
- **c.** Store as a learned clause.

### Task T1.9.E7.3 — Consumption

In hypgen, before emitting a candidate, check if adding it
would make any learned clause fully-satisfied → reject.

### Task T1.9.E7.4 — Tests + bench

Hard puzzles: zebra2 + harder. Bench vs E20 (which persists
across `solve()` calls).

## Acceptance

- Learned clauses stored, consumed by hypgen.
- Bench shows ≥ 20% node-count reduction on a contradiction-
  heavy puzzle.

## References

CDCL; ATMS justification cache.

## Cross-links

- [P1.9 README — E7](README.md#conflict-driven-learning-satcdcl-inspired).
- [E6](s1.9.e6_transitive_premise_walk.md) — generator.
- [E19](s1.9.e19_unsat_core_min.md) — minimiser.
- [E20](s1.9.e20_conflict_cache.md) — cross-call persistence.
- [docs/lib/02](../../../docs/lib/02-solvers-csp-sat-smt.md),
  [docs/lib/09](../../../docs/lib/09-cognitive-architectures-neurosymbolic.md).

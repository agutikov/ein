; S1.9.E2 — (at-most-one R slot) activator. Per-slot
; cardinality declaration; closure derives when every leaf at
; that slot is occupied.

# S1.9.E2 — `(at-most-one R slot)` activator

**Phase:** P1.9, §closure refinements.
**Estimate:** M (1-3 days).
**Value:** M.
**Status:** 📅 parked.
**Catalog row:** [P1.9 README — Closure refinements](README.md#closure-refinements-q-s154b-descendants)
E2.

## Context

Generalises `functional` to declare cardinality per slot. E.g.,
`(at-most-one R 0)` says "for any value at slot 1, there's ≤ 1
distinct value at slot 0 paired with it".

Once every leaf at the declared slot has a binding, closure
(`(closed R)`) derives — the activator becomes a CSP-style
cardinality constraint.

## Tasks

### Task T1.9.E2.1 — Surface syntax

`(at-most-one R slot)` as a property fact.

### Task T1.9.E2.2 — Closure derivation rule

A stdlib rule that derives `(closed R)` when the cardinality
is saturated.

### Task T1.9.E2.3 — Use case smoke

A puzzle that uses `(at-most-one R …)` solves correctly.

## Acceptance

- Activator parses; closure derivation rule fires correctly.

## References

CSP cardinality constraints.

## Cross-links

- [P1.9 README — E2](README.md#closure-refinements-q-s154b-descendants).
- [E1](s1.9.e1_functional_activator.md) — narrower form.
- [S1.8.A6 closure auto-inference](../p1.8_ein_lang_modules/s1.8.a6_closure_auto_inference.md).
- [docs/index/02 solvers / CSP / SAT / SMT](../../../docs/index/02-solvers-csp-sat-smt.md).

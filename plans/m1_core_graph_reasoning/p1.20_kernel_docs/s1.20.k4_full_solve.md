; S1.20.K4 — The whole puzzle + integration. The guide's closing chapter:
; assemble the pieces into the full zebra2, run `ein solve`, read the answer,
; and HAND OFF to zebra_walkthrough.md for the complete deductive trace.
; Plus the docs/guide/ wiring: reading order, repo "start here", and fixing
; the stale Theme-K cross-ref left by the walkthrough relocation.

# S1.20.K4 — Guide: solving the whole puzzle + integration

**Phase:** P1.20 (kernel documentation), Theme K (stage 4 of 4).
**Estimate:** 1.5-2 days.
**Status:** **executed 2026-06-17** — created
[`docs/guide/04_solving_the_whole_puzzle.md`](../../../docs/guide/04_solving_the_whole_puzzle.md)
(assemble `zebra2` → **verified** `ein solve` output → hand off to
`zebra_walkthrough.md` + `docs/api/`; "your turn" capstone). Wired
`docs/guide/` into the repo README (Layout + Quickstart), the kernel README
(newcomer audience row + path), `AGENTS.md`, and a backlink from the
walkthrough. The stale Theme-K `examples/README.md` cross-ref was already
fixed by the walkthrough relocation.
**Depends on:** [K1](s1.20.k1_onramp.md)–[K3](s1.20.k3_rule_families.md)
(the chapters this assembles + hands off from).
**Blocks:** nothing — closes Theme K and P1.20.

## Context

The reader has met objects/facts (K1), simple rules (K2), and the hard
families (K3). K4 puts it together on the real puzzle, runs it, and **hands
off to the full solved trace** — making `docs/guide/` and
[`zebra_walkthrough.md`](../../../docs/kernel/inference/zebra_walkthrough.md)
the designed complement the user described: the guide *teaches* the pieces;
the walkthrough *shows them all firing* end-to-end.

## Tasks

### Task T1.20.K4.1 — Chapter `docs/guide/04_solving_the_whole_puzzle.md`

- Assemble: the full [`zebra2.ein`](../../../examples/zebra2.ein) =
  imports (stdlib property rules, K2/K3) + the puzzle facts + the spatial
  rules (K3) + the `(query …)` goal. Walk its *structure*, not every line.
- Run it: `ein solve examples/zebra2.ein` → read the verdict + the
  rendered answer (Norwegian/water, Japanese/zebra). Show
  `--trace out.md` and `--exhaustive` (certify unique) as the
  reader's next moves.
- **Hand off** to [`zebra_walkthrough.md`](../../../docs/kernel/inference/zebra_walkthrough.md)
  for the full step-by-step deductive trace, and to
  [`docs/api/`](../../../docs/api/) for driving the solve from Python — the
  guide's "where to go next."
- Keep the boundary: point at the inference docs for *how the engine
  searches*; the guide never explains it.

### Task T1.20.K4.2 — Wire `docs/guide/` into the repo

- Reading-order + audience in `docs/guide/README.md` finalised (K1 → K4).
- Add the **"start here / learn Ein"** pointer to the repo
  [`README.md`](../../../README.md) and the kernel
  [`README.md`](../../../docs/kernel/README.md) audience table (a
  **newcomer/tutorial** entry that precedes the user/dev/embedder paths).
- Cross-link from `zebra_walkthrough.md` back to the guide ("new to Ein?
  start with the guide") — make the complement bidirectional.

### Task T1.20.K4.3 — Reconcile the stale Theme-K cross-ref

The Theme K spec's NL-framing bullet still says "cross-link
`examples/README.md`" (pre-relocation prose). Update the README's Theme K
section + the **acceptance line** to the `docs/guide/` reality and the
walkthrough's new path.

## Acceptance

- `docs/guide/04_…` solves the whole puzzle and hands off to the
  walkthrough + api docs; the guide reads end-to-end K1 → K4 as a
  newcomer tutorial.
- `docs/guide/` is reachable from the repo README + kernel README as the
  newcomer entry point; the guide ↔ walkthrough complement is bidirectional.
- A reader finishing the guide can author *and* solve a small puzzle of
  their own, and knows where to go for internals (kernel) and embedding (api).

## Open questions

- **Q-S1.20.K4.A — "your turn" capstone.** Whether to end with a tiny
  *unsolved* exercise (author a 3-house puzzle, solve it) as the capstone.
  **Default: yes, a short optional exercise** — it's the "by example"
  payoff; keep it small so it doesn't rot.

## Cross-links

- [K1](s1.20.k1_onramp.md)–[K3](s1.20.k3_rule_families.md) — the chapters
  this closes.
- [`zebra_walkthrough.md`](../../../docs/kernel/inference/zebra_walkthrough.md)
  — the full trace handed off to (bidirectional complement).
- [`docs/api/ein.md`](../../../docs/api/ein.md) — the Python embedding
  next-step.
- [P1.20 README — Theme K](README.md#theme-k--ein-zebra-guide) — the
  acceptance line this stage reconciles.

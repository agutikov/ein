; S1.20.I4 — features.md write-up. Turn I3's measured artifact into the
; reference page: per-lever rows [default / off / slowdown × / broken-if-off],
; split correctness-load-bearing vs perf-only, cross-linked to the engine
; narrative (Theme D) and the SolverConfig definitional table (Theme J).

# S1.20.I4 — `features.md` result table + narrative

**Phase:** P1.20 (kernel documentation), Theme I (stage 4 of 4).
**Estimate:** 1 day.
**Status:** **executed 2026-06-17** — wrote
[`docs/kernel/inference/features.md`](../../../docs/kernel/inference/features.md)
(fast-path + exhaustive tables, per-lever notes, top takeaway, PyPy/SHA-
stamped provenance); added it to the inference README file list and
repointed the [`docs/api/inference.md`](../../../docs/api/inference.md)
forward-link at it. Honest headline: on zebra2 the fast path is lever-
insensitive; only `enable_singleton_writeback` is load-bearing (exhaustive).
**Depends on:** [S1.20.I3](s1.20.i3_bench_sweep.md) (the measured artifact)
and [S1.20.I1](s1.20.i1_config_audit.md) (the matrix spec + classification).
**Blocks:** nothing — closes Theme I.

## Context

The deliverable: a dev-facing page that answers "which knobs do I need
on?" with measured numbers. Lands as
`docs/kernel/inference/features.md` (alongside the engine docs Theme D
shipped).

## Tasks

### Task T1.20.I4.1 — Write `docs/kernel/inference/features.md`

- **Audience banner: dev** (a puzzle author cares about the *conclusion* —
  "keep these on"; the matrix itself is engine-facing).
- **Two tables**, per I1's classification:
  - **Correctness-load-bearing** — flag, what breaks when off (wrong
    verdict / non-termination → ∞ sentinel). These are non-negotiable.
  - **Perf-only** — flag, default-on solve, flag-off solve, **slowdown ×**,
    + the `MonotonicStats` deltas (enterings, layers) that explain *why*.
- **Value-choice sub-section** — `hypgen_scoring` (popularity vs
  most-constrained) and `lattice_order` (lex vs score-sum) as swaps, not
  on/off.
- **Provenance line** — host, interpreter (PyPy), commit SHA, "indicative
  not absolute" (numbers are machine-specific; from I3's artifact).

### Task T1.20.I4.2 — Cross-link both directions

- Into the engine narrative: [Theme D](s1.20.d_inference_engine_docs.md)'s
  [`architecture_and_algorithms.md`](../../../docs/kernel/inference/architecture_and_algorithms.md)
  / [`python_impl.md`](../../../docs/kernel/inference/python_impl.md) (the
  per-feature *how*, this page's *how-much*).
- To the definitional side: Theme J's
  [`docs/api/inference.md`](../../../docs/api/inference.md) SolverConfig
  table (which already forward-links here).
- From the inference [`README.md`](../../../docs/kernel/inference/README.md)
  file list so the page is discoverable.

### Task T1.20.I4.3 — The author-facing takeaway

A one-paragraph "what to keep on" summary at the top (the thing a puzzle
author actually reads) before the detailed tables.

## Acceptance

- `features.md` carries both tables with measured impact per lever,
  separating correctness-load-bearing from perf-only, with the
  value-choice swaps and a machine/SHA-stamped provenance line.
- Cross-links resolve both ways (engine narrative ↔ definitional table).
- A reader knows, from the top paragraph alone, which knobs are mandatory.

## Open questions

- **Q-S1.20.I4.A — Refresh policy.** The numbers drift as the engine
  evolves. **Default: stamp with SHA + "indicative"**, regenerate via I3's
  harness when the engine changes materially (mirrors Theme D's
  per-file "verified for SHA" convention).

## Cross-links

- [P1.20 README — Theme I](README.md#theme-i--kernel-feature--config-matrix)
  — phase context + acceptance.
- [S1.20.I3](s1.20.i3_bench_sweep.md) — the artifact this renders.
- [S1.20.D](s1.20.d_inference_engine_docs.md) /
  [`docs/api/inference.md`](../../../docs/api/inference.md) — the narrative
  + definitional pages this table sits between.

; S1.20.H1 — Differentiate atoms (names) and objects (nodes)
; in the ein model. Atoms = `rule`, `not`, `T`, `relation`,
; `Alice`, `co-located` (lexical tokens). Objects = Levi-
; bipartite graph nodes with no out-arrows and in-arrows from
; facts. The refinement promotes "atom" to a 5th term in the
; foundational table.

# S1.20.H1 — Ein-model: atoms vs objects

**Phase:** P1.20 (kernel documentation), Theme H.
**Estimate:** 1 day — writing + glossary updates.
**Status:** parked; sequential with the rest of Theme H but
the simplest item.
**Depends on:** nothing structural; can run any time.
**Blocks:** [S1.20.H2](s1.20.h2_four_level_kb.md) (the 4-level
KB representation uses the atom/object distinction).

## Context

User direction 2026-05-24:

> *"atoms are names — `rule`, `not`, `T`, `relation`, `Alice`,
> `co-located` are all atoms. Objects are, as before,
> Levi-bipartite graph nodes without out-arrows and with
> in-arrows from facts."*

The current [`docs/kernel/ir/01-ein-graph/03_ein_model.md`](../../../docs/kernel/ir/01-ein-graph/03_ein_model.md)
§2 ("four foundational terms") lists *node, arrow, object,
relation*. This stage promotes **atom** to a fifth term,
distinguished from **object**:

| term     | refined definition (2026-05-24)                                          |
|----------|--------------------------------------------------------------------------|
| **atom** | a *name* — a lexical token used to identify nodes. `rule`, `not`, `Alice`, `co-located` are all atoms. |
| **object** | a *node* — a graph vertex with no out-arrows and with in-arrows from facts. Identified by an atom. |
| **relation** | (unchanged) a node containing two or more outbound arrows. |
| **node**, **arrow** | (unchanged) |

Relation between atom and object: an atom *labels* an object
node. Two occurrences of the same atom across the KB identify
the same object node (per the identity rule §2). The atom is
the *name*; the object is the *thing named*.

Why it matters: the current model treats `rule`, `relation`,
`not` as atoms-acting-as-relation-heads but doesn't have a clean
word for "the name itself". This refinement gives one. Bears on
[F5 — rules-as-data](../../followups/f5_rules_as_data.md) where
atoms-as-first-class become important.

## Tasks

### Task T1.20.H1.1 — Update `03_ein_model.md`

- **a.** §2 "The four foundational terms" → "The five
  foundational terms"; add the **atom** row with the
  refinement above.
- **b.** §3 "Two flavours of node" — clarify that "named"
  nodes are *labelled by atoms*; the atom is the lexical
  token, the node is the graph vertex.
- **c.** Update §4 ("The 'relation' problem") to use the
  atom/object distinction consistently.

### Task T1.20.H1.2 — Glossary updates

`docs/kernel/glossary.md`:

- **a.** Add an `atom` entry.
- **b.** Refine the `object` entry to clarify atom-vs-object.
- **c.** Add an `identifier` cross-reference if relevant.

### Task T1.20.H1.3 — Worked example

Update [`docs/kernel/ir/01-ein-graph/04_jack_drinks_coffee.md`](../../../docs/kernel/ir/01-ein-graph/04_jack_drinks_coffee.md)
(the reflexive-algebra worked example) to use the atom/object
language. The example's content should not change — just the
terminology.

### Task T1.20.H1.4 — Cross-refs

- **a.** Any other doc that defines / uses "object" or
  "atom" — make terminology consistent.
- **b.** Source code: no rename needed; the Python data model
  doesn't need to grow an `Atom` class for M1. (Could be
  revisited under [F5](../../followups/f5_rules_as_data.md).)

## Acceptance

- `03_ein_model.md` §2 lists five terms, one of which is
  `atom`.
- Glossary has the atom entry; object entry is refined.
- The worked example uses the new terminology.
- No code change in M1 — this is a vocabulary update.

## Open questions

- **Q-S1.20.H1.A — Atom in the Python data model?** Could be
  a separate `Atom` class with `name: str` + identity-by-name;
  M1's current `Instance` and `Type` both effectively *are*
  this. Re-revisit under F5.
- **Q-S1.20.H1.B — Names vs identifiers.** Are atoms = names
  = identifiers? Probably yes; document the equivalence.

## Cross-links

- [P1.20 README — Theme H](README.md#theme-h--ein-model-update-atoms-vs-objects-4-levels)
  — phase context.
- [`docs/kernel/ir/01-ein-graph/03_ein_model.md`](../../../docs/kernel/ir/01-ein-graph/03_ein_model.md)
  — the primary target.
- [`docs/kernel/glossary.md`](../../../docs/kernel/glossary.md)
  — the glossary updates.
- [S1.20.H2](s1.20.h2_four_level_kb.md) — depends on this stage's
  vocabulary.
- [F5 — rules as data](../../followups/f5_rules_as_data.md) —
  long-term: atoms become first-class data.

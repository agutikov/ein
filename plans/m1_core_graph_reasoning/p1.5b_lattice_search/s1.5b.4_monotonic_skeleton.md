; S1.5b.4 — Monotonic engine skeleton. Creates the
; `inference/monotonic/` module structure with stub functions
; that raise NotImplementedError + a `bench_monotonic.py`
; skeleton that wires arg-parsing to the stub. Pytest collects
; new tests; new tests skip with `pytest.skip("backbone")`.
; The deliverable is import-clean infrastructure that S1.5b.5
; fills in.

# S1.5b.4 — Monotonic engine skeleton

**Phase:** P1.5b (set-indexed search engines).
**Estimate:** XS (½ day) — pure scaffolding.
**Status:** **shipped 2026-05-27** — 3 stub modules + bench
CLI + 2 test files; 3 new tests pass, 2 backbone-placeholder
tests skip with `pytest.skip("backbone — implemented in
S1.5b.5")`; ruff clean; full suite 724 passed + 3 skipped
(was 721+1; +3 passing scaffolding + 2 new backbone skips).
Spec implemented verbatim; the `# noqa: E402` and
`# noqa: ANN001` directives in the spec pseudocode were
auto-removed by `ruff --fix` as unused (the linter doesn't
trigger E402 for the `sys.path.insert` idiom here, and ANN001
isn't enabled in this project's ruff config).
**Depends on:**
[S1.5b.1](s1.5b.1_file_split_refactor.md) (directory exists),
[S1.5b.2](s1.5b.2_common_apriori_gen.md) (Apriori-gen
available for import),
[S1.5b.3](s1.5b.3_common_set_batch_primitive.md) (`try_commitment_set`
available for import).
**Blocks:** [S1.5b.5](s1.5b.5_monotonic_backbone.md)
(backbone fills these stubs).

## Goal

Stand up the monotonic engine's directory + module structure
+ CLI entry point so that S1.5b.5 can land the backbone
implementation without any scaffolding distractions.

After this stage:

- `ein.py/src/ein_bot/inference/monotonic/` has the stub
  modules.
- `ein.py/demo/bench_monotonic.py` exists and accepts the
  arg-set we want.
- `ein.py/tests/inference/monotonic/` collects new tests
  (all marked `pytest.skip` until S1.5b.5).
- The 670+ existing test suite stays green.

## Module surface (stubs)

### `inference/monotonic/__init__.py`

```python
"""Monotonic set-search engine.

Converges to the first met solution if any. Single
KnowledgeBase instance (root); commitment sets entered via
the common `try_commitment_set` primitive; unconditional facts merged
into root; terminates on `is_solved(root.kb)` becoming True.

SOLVE mode only. See [P1.5b README] for the design rationale
and the equivalence claim against the lattice engine.
"""
from ein_bot.inference.monotonic.solver import monotonic_solve

__all__ = ["monotonic_solve"]
```

### `inference/monotonic/solver.py`

```python
"""Monotonic engine main loop. Stub — backbone in
[S1.5b.5](../../../plans/m1_core_graph_reasoning/p1.5b_lattice_search/s1.5b.5_monotonic_backbone.md).
"""
from __future__ import annotations

from typing import Any

from ein_bot.inference.config import SolverConfig
from ein_bot.kb.store import KnowledgeBase

Verdict = Any  # placeholder; the real types live in
               # `inference/tree/solver.py` (Solution /
               # Ambiguity / Contradiction). S1.5b.5 either
               # imports them or defines monotonic-specific
               # variants.

def monotonic_solve(
    root_kb: KnowledgeBase,
    *,
    max_set_size: int = 5,
    config: SolverConfig | None = None,
) -> Verdict:
    """Run the monotonic set-search engine on root_kb.

    Stub — backbone lands in S1.5b.5.
    """
    raise NotImplementedError(
        "monotonic_solve — backbone in S1.5b.5"
    )
```

### `inference/monotonic/state_dump.py`

```python
"""Monotonic dumper — minimal per-layer root snapshots.

Stub — implementation in S1.5b.7 (after the backbone is
running so we know what's worth dumping).
"""
from __future__ import annotations

from pathlib import Path

from ein_bot.kb.store import KnowledgeBase

class MonotonicDumper:
    """Dumps root snapshots per layer + a 00_timeline.jsonl
    of every entering's outcome.

    Stub — see S1.5b.7.
    """
    def __init__(self, out_dir: Path) -> None:
        self.out_dir = out_dir
        self.out_dir.mkdir(parents=True, exist_ok=True)

    def root_initial(self, kb: KnowledgeBase) -> None:
        raise NotImplementedError("S1.5b.7")

    def layer_start(self, layer: int) -> None:
        raise NotImplementedError("S1.5b.7")

    def entering(self, commitment, result) -> None:  # noqa: ANN001
        raise NotImplementedError("S1.5b.7")

    def layer_end(self, layer: int, kb: KnowledgeBase) -> None:
        raise NotImplementedError("S1.5b.7")

    def summary(self, verdict) -> None:  # noqa: ANN001
        raise NotImplementedError("S1.5b.7")
```

## CLI entry point — `bench_monotonic.py`

`ein.py/demo/bench_monotonic.py`:

```python
"""Run the monotonic set-search engine on a .ein file.

Mirrors `bench_solve.py`'s CLI shape but reaches into
`inference/monotonic/` instead of `inference/tree/`. Output
is one-shot: print the verdict + bindings + a brief
per-layer summary; with `--dump-states DIR` produce a
minimal monotonic dump (root snapshot per layer +
00_timeline.jsonl).

Stub — backbone wired in S1.5b.5.
"""
from __future__ import annotations

import argparse
import sys
from pathlib import Path

# Make `from ein_bot.…` resolve when running from a checkout.
sys.path.insert(
    0, str(Path(__file__).resolve().parents[1] / "src"),
)

from ein_bot.inference.config import SolverConfig                       # noqa: E402
from ein_bot.inference.monotonic.solver import monotonic_solve          # noqa: E402
from ein_bot.inference.monotonic.state_dump import MonotonicDumper      # noqa: E402
from ein_bot.ir import parse                                             # noqa: E402
from ein_bot.kb.store import KnowledgeBase                               # noqa: E402

def _build_argparser() -> argparse.ArgumentParser:
    ap = argparse.ArgumentParser(
        description=__doc__.split("\n", 1)[0],
    )
    ap.add_argument("puzzle", type=Path,
                    help="path to .ein puzzle file")
    ap.add_argument("--max-set-size", type=int, default=5,
                    help="largest commitment size to enumerate "
                         "(default: 5)")
    ap.add_argument("--dump-states", type=Path, default=None,
                    help="if set, write a minimal monotonic "
                         "dump to this directory")
    ap.add_argument("--max-time", type=float, default=None,
                    help="abort after N seconds")
    ap.add_argument("--verbose", "-v", action="store_true",
                    help="per-layer progress")
    return ap

def main(argv: list[str] | None = None) -> int:
    args = _build_argparser().parse_args(argv)
    text = args.puzzle.read_text()
    kb = KnowledgeBase.from_ir(parse(text))

    config = SolverConfig()  # default; flags can extend later

    dumper = (
        MonotonicDumper(out_dir=args.dump_states)
        if args.dump_states is not None else None
    )
    # NOTE — S1.5b.5 wires `dumper` into `monotonic_solve`'s
    # signature; current stub doesn't accept it.

    verdict = monotonic_solve(
        kb,
        max_set_size=args.max_set_size,
        config=config,
    )

    print(f"verdict: {type(verdict).__name__}")
    return 0

if __name__ == "__main__":
    sys.exit(main())
```

## Test scaffolding

`ein.py/tests/inference/monotonic/__init__.py` — empty
(created in S1.5b.1).

`ein.py/tests/inference/monotonic/test_monotonic_skeleton.py`:

```python
"""S1.5b.4 skeleton — imports + arg-parsing smoke. The real
test surface lands in S1.5b.5 (backbone) + S1.5b.9 (parity).
"""
import pytest

from ein_bot.inference.monotonic import monotonic_solve
from ein_bot.inference.monotonic.solver import monotonic_solve as _direct
from ein_bot.inference.monotonic.state_dump import MonotonicDumper

def test_imports_resolve():
    assert monotonic_solve is _direct
    assert MonotonicDumper is not None

def test_solver_raises_not_implemented():
    """Until S1.5b.5 lands."""
    from ein_bot.kb.store import KnowledgeBase
    kb = KnowledgeBase()
    with pytest.raises(NotImplementedError):
        monotonic_solve(kb, max_set_size=1)

@pytest.mark.skip(reason="backbone — implemented in S1.5b.5")
def test_zebra_solves(): ...

@pytest.mark.skip(reason="backbone — implemented in S1.5b.5")
def test_branching_demos_parity(): ...
```

`ein.py/tests/test_bench_monotonic.py`:

```python
"""S1.5b.4 skeleton — bench script smoke.

The script's arg-parsing + import surface should work even
though `monotonic_solve` raises NotImplementedError. Once
S1.5b.5 lands, this can run a real puzzle.
"""
import subprocess
import sys
from pathlib import Path

REPO = Path(__file__).resolve().parents[1]
SCRIPT = REPO / "demo" / "bench_monotonic.py"

def test_help_smoke():
    """--help works → arg-parsing is wired."""
    proc = subprocess.run(
        [sys.executable, str(SCRIPT), "--help"],
        capture_output=True, text=True, check=True,
    )
    assert "monotonic" in proc.stdout.lower()
    assert "--max-set-size" in proc.stdout
    assert "--dump-states" in proc.stdout
```

## Tasks

### T1.5b.4.1 — Create the stub modules

Write `__init__.py`, `solver.py`, `state_dump.py` per the
surface above. Every public function raises
`NotImplementedError` referencing the stage that fills it.

### T1.5b.4.2 — Author `bench_monotonic.py`

Mirror `bench_solve.py`'s CLI surface; reach into
`inference.monotonic.solver` instead of tree. Document the
stub status in the file header.

### T1.5b.4.3 — Test scaffolding

Add the two test files above. Mark backbone tests with
`pytest.mark.skip(reason="backbone — implemented in S1.5b.5")`.

### T1.5b.4.4 — Ruff + suite

`ruff check`; full pytest:
- 670 existing tests still green.
- 2 new "imports resolve" tests pass.
- 1 new "solver raises NotImplementedError" test passes.
- Other monotonic tests skip with the documented reason.
- 1 new `bench_monotonic.py --help` smoke test passes.

## Acceptance

- `inference/monotonic/` has `__init__.py`, `solver.py`,
  `state_dump.py` with public surface documented +
  `NotImplementedError`-raising stubs.
- `ein.py/demo/bench_monotonic.py` accepts the argument set
  (`puzzle`, `--max-set-size`, `--dump-states`, `--max-time`,
  `--verbose`) and prints "verdict: …" (after the stub
  raises if a puzzle is supplied — that's fine for now).
- `tests/inference/monotonic/test_monotonic_skeleton.py`
  + `tests/test_bench_monotonic.py` ship with the imports
  + skip markers.
- `ruff check` clean.
- Full suite green (existing + 3 new passing + 2 skips).

## Cross-links

- Module structure: matches the
  [S1.5b.0 § Co-existence layout](s1.5b.0_decisions_log.md#co-existence-layout)
  + [Q1.5b.1.a](open_questions.md#q15b1--co-existence-layout)
  resolution.
- Filling stages:
  - [S1.5b.5](s1.5b.5_monotonic_backbone.md) — backbone
    fills `monotonic_solve`.
  - [S1.5b.7] — monotonic dumper implementation.
- Shared imports (from common):
  - [`inference/apriori.py`](../../../ein.py/src/ein_bot/inference/apriori.py)
    — Apriori-gen.
  - [`inference/commitment.py`](../../../ein.py/src/ein_bot/inference/commitment.py)
    — `try_commitment_set`.
  - [`inference/nogoods.py`](../../../ein.py/src/ein_bot/inference/nogoods.py)
    — CDCL store + helpers.

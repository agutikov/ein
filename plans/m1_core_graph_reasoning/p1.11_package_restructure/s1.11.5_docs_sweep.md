; S1.11.5 — Docs/plans/examples sweep: update every
; ein_bot / ein.py/demo/ reference under docs/, examples/,
; plans/, README.md, CLAUDE.md, AGENTS.md.

# S1.11.5 — Docs / plans / examples sweep

**Phase:** P1.11 (package + CLI restructure).
**Estimate:** ½-1 day — bulk grep + edit + double-check.
**Status:** **name-sweep DONE 2026-06-15** (commit `e49a325`) —
every `ein_bot`/`ein-bot` reference under docs/, examples/,
plans/, README.md, AGENTS.md (+ CLAUDE.md symlink) rewritten to
`ein`/`Ein`. Ran in the same sweep as S1.11.2, *not* after
S1.11.3/S1.11.4 (those haven't shipped). **Still pending:** the
`python ein.py/demo/...` → merged-CLI path updates (need S1.11.3)
and the split-CLI surface docs (need S1.11.4); revisit this stage
when they land.
**Depends on:** S1.11.1–S1.11.4 land.
**Blocks:** [S1.11.6](s1.11.6_acceptance.md).

## Context

The repo's documentation cross-references `ein_bot`, `ein.py/`,
and `python ein.py/demo/...` paths in many places. This stage
sweeps every doc/plan/example/instruction file and updates them
to the new `ein` package + CLI surface.

## Tasks

### Task T1.11.5.1 — Inventory

- **a.** `grep -rln "ein_bot\|ein\.py/demo\|src/ein_bot"
  docs/ plans/ examples/ README.md CLAUDE.md AGENTS.md
  ein.py/`. Tabulate file hits.
- **b.** Categorise:
  - Imports / CLI invocations → rewrite to new surface.
  - Historical context (e.g. "ein-bot was the placeholder
    name") → keep, optionally clarify with date.
  - Path references (`ein.py/src/ein_bot/X`) → rewrite to new
    paths.

### Task T1.11.5.2 — Per-tree sweep

- **a.** `docs/`: the kernel docs, ideas, index — rewrite
  references; verify cross-link integrity.
- **b.** `plans/`: every README + stage file. Most refer via
  relative paths or are short.
- **c.** `examples/`: comments in .ein files that cite the CLI.
- **d.** Top-level: `README.md`, `CLAUDE.md`, `AGENTS.md` (the
  symlink) — keep the symlink intact.
- **e.** `ein.py/`: `tests/` import paths handled by S1.11.2;
  any docstring rewrites missed there.

### Task T1.11.5.3 — Test fixtures + golden files

If pytest has any golden-file fixtures that include
package-name strings (e.g., a `--help` output snapshot), update
them.

### Task T1.11.5.4 — Final grep

- `grep -rln "ein_bot" .` returns only entries the user
  explicitly wants kept (historical context, commit messages).
- Same for `ein.py/demo/`.

## Open questions

- **Q-S1.11.5.A — Generated artefacts.** Do any auto-generated
  files (e.g. `egg-info/`, `__pycache__/`) cache the old name?
  Probably yes; clean them and regenerate.

## Cross-links

- [P1.11 README](README.md) — phase context.
- [S1.11.2](s1.11.2_codebase_rename.md) — sibling sweep for
  code; this one is the doc complement.
- [S1.11.6](s1.11.6_acceptance.md) — terminal acceptance.

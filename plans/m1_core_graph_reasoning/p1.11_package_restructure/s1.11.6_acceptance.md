; S1.11.6 — Acceptance: rename ships when pytest + ruff are
; green, zebra E2E passes against the renamed package, and CLI
; surface is documented + working.

# S1.11.6 — Acceptance

**Phase:** P1.11 (package + CLI restructure).
**Estimate:** ½ day — pure measurement + regression.
**Status:** **DONE 2026-06-16.** Full acceptance green after S1.11.3 +
S1.11.4: unit suite **1287 passed** (`EIN_RUN_SLOW=1`, was 1283 + the 4
slow zebra tests) / **acceptance gate 8 passed** incl. `test_bench_solve_mode`
(now driving `ein search`) + `test_cli_solve_emits_answer_in_words`
(T1.11.6.1a, .3); `ruff check .` clean (T1.11.6.1b); `ein --help` lists all
9 subcommands, `ein solve` / `ein search` / `ein saturate` / `ein lattice`
smoke OK, `python -m ein.cli` + the `ein` console script + `from ein.cli
import main` all resolve to the package (T1.11.6.2); `ein.py/demo/` removed,
no stale `demo/` refs outside archival `plans/` (T1.11.6.4). `ein_bot` clear
since 2026-06-15.
**Depends on:** all of S1.11.1–S1.11.5.

## Context

This stage bundles regression + acceptance for the entire
P1.11 rename. No new design; just confirm the rename didn't
break anything.

## Tasks

### Task T1.11.6.1 — Test suite

- **a.** `pytest ein.py/tests/` — green, count matches
  pre-rename count.
- **b.** `ruff check src/ein/ tests/` — green.
- **c.** `pip install -e ein.py/` works in a clean venv.

### Task T1.11.6.2 — CLI smoke

- **a.** `ein --help` lists all subcommands.
- **b.** `ein solve examples/zebra2.ein` works.
- **c.** `ein bench solve examples/zebra2.ein` works.
- **d.** `ein saturate examples/saturation/transitive/taxonomy.ein`
  works.

### Task T1.11.6.3 — M1 acceptance re-run

- **a.** Zebra E2E (M1 acceptance #2 + #3) passes against
  the renamed package — same verdict, same trace structure.
- **b.** Three-task-classes mode flags work
  (M1 acceptance #4).

### Task T1.11.6.4 — Stale references

- **a.** `grep -rln "ein_bot\|ein\.py/demo" .` — returns only
  intentional historical refs (commit messages, archival
  notes). ✅ `ein_bot` clear 2026-06-15; `ein.py/demo` paths
  remain until S1.11.3 merges the demos.
- **b.** Top-level README + CLAUDE.md cite the new surface.

## Acceptance

- All four task batches green.
- `git log --oneline` shows the P1.11 chain of stage commits
  in expected order.
- The user can use `ein` as the project name everywhere
  without breaking imports / CLI / docs.

## Cross-links

- [P1.11 README](README.md) — phase context.
- [M1 acceptance](../README.md#acceptance) — re-validated in
  T1.11.6.3.
- All preceding S1.11.* stages.

; S1.11.3 — Demo merge: move ein.py/demo/bench_*.py into the
; package proper (src/ein/cli/bench.py or src/ein/bench/) and
; expose as CLI subcommands.

# S1.11.3 — Demo merge

**Phase:** P1.11 (package + CLI restructure).
**Estimate:** ½ day.
**Status:** **DONE 2026-06-16.** Reality diverged from the plan: `demo/`
held **13** scripts, not the 2 (`bench_saturate` + a non-existent
`bench_solve`) assumed here. Resolution (per user direction — utility vs
core, not "benchmark"): the 5 durable engine-runners were promoted to
top-level `ein` subcommands — `bench_saturate`→`ein saturate`,
`bench_monotonic`→**`ein search`** (not `ein solve`, which already exists
as the answer/trace command), `bench_lattice`→`ein lattice`,
`profile_solve`→`ein profile`, `bench_symmetric`→`ein symmetric` — and the
8 one-off probe/measure scripts moved to repo-root `utils/`. CLI dispatch
delegates to each module's `main(argv)` (intercepted before argparse to
dodge REMAINDER's leading-`-` bug), so the promoted scripts' output stayed
byte-identical and the two subprocess tests only needed repointing to
`python -m ein.cli search`. Q-S1.11.3.A resolved **no** (no compat shim).
**Depends on:** [S1.11.2](s1.11.2_codebase_rename.md) — imports
must be clean before moving demo code in; [S1.11.4](s1.11.4_cli_split.md)
**parallel** because both this and CLI split touch the
`src/ein/cli/` directory structure.
**Blocks:** [S1.11.6](s1.11.6_acceptance.md).

## Context

`ein.py/demo/bench_saturate.py` and `bench_solve.py` are
runnable only via `python ein.py/demo/X.py`. They contain
useful benchmark infrastructure that belongs *inside* the
package, exposed as CLI subcommands `ein bench solve …` /
`ein bench saturate …`.

## Tasks

### Task T1.11.3.1 — Decide module layout

Two options:

- **(a) Flat under cli/:** `src/ein/cli/bench_solve.py` +
  `bench_saturate.py`. Simple; consistent with one-file-per-
  subcommand from S1.11.4.
- **(b) Sub-package `bench/`:** `src/ein/bench/{solve,saturate}.py`
  with a thin wrapper in `src/ein/cli/bench.py`. Cleaner if
  bench grows past two scripts.

**Recommend (a)** for M1; promote to (b) if bench grows.

### Task T1.11.3.2 — Move + integrate

- **a.** `git mv ein.py/demo/bench_solve.py
  ein.py/src/ein/cli/bench_solve.py` (or sub-package per (b)).
- **b.** Refactor: remove `if __name__ == "__main__":` boiler
  if present; expose a `main(argv)` function.
- **c.** Wire as CLI subcommand: `ein bench solve PATH …`
  invokes `bench_solve.main([…])`.
- **d.** Same for `bench_saturate`.

### Task T1.11.3.3 — Remove the old demo directory

- **a.** `git rm -r ein.py/demo/`.
- **b.** Note in a top-level doc (README / CLAUDE.md / docs)
  that bench commands are now `ein bench …` not `python
  ein.py/demo/…`.

### Task T1.11.3.4 — Acceptance

- `ein bench solve examples/zebra2.ein` works and produces
  the same output as `python ein.py/demo/bench_solve.py
  examples/zebra2.ein` did pre-merge.
- `ein bench saturate examples/saturation/transitive/taxonomy.ein`
  same.
- `python ein.py/demo/...` paths return file-not-found.
- Documentation cross-references updated.

## Open questions

- **Q-S1.11.3.A — Backward-compat shim.** Keep a deprecated
  wrapper script at `ein.py/demo/bench_solve.py` that warns
  and forwards to the new CLI? Probably no — the path was
  developer-internal, no external consumers. Confirm.

## Cross-links

- [P1.11 README](README.md) — the phase context.
- [S1.11.4](s1.11.4_cli_split.md) — parallel work on the CLI
  folder structure; coordinate the file layout.
- [S1.11.6](s1.11.6_acceptance.md) — terminal acceptance bundle.

# S1.21.4 — Formal semantics of `absent` (NAF) — dedicated doc

**Phase:** P1.21 (review response). **Priority: P1** — the system's most
load-bearing boundary is undocumented.
**Review point:** [REVIEW_M1-01 §4](../REVIEW_M1-01.md).
**Depends on:** nothing (investigation read-only; improvement gated on
[`reports/r4_absent_semantics.md`](reports/r4_absent_semantics.md)).
S1.21.2 and S1.21.6 lean on this stage's definitions — coordinate wording.

## The claim

`absent` is what splits Ein into *positive monotone reasoning* + *non-monotone
observations of absence*, and its consequences are already scattered through
the implementation as hard-won local decisions:

- fork-derived facts can't be back-propagated to root
  ([`solver.py:376-379`](../../../ein.py/src/ein/inference/monotonic/solver.py),
  S1.21.2);
- positive-premise provenance is insufficient for dependence
  (S1.21.2's counterexample);
- deletion-based MUS minimisation is unsound
  ([`frontier.py`](../../../ein.py/src/ein/inference/frontier.py) —
  `min_core.py` until the S1.21.3 rename);
- saturation needs fire-time re-evaluation of `AbsentGuard`;
- semi-naive triggering must watch relation changes *inside* `AbsentGuard`.

The review: **formalise the semantics in its own document before extending
the language.** Candidate meanings of `KB ⊨ absent(P)`:

1. **Closed-world:** `P ∉ closure(KB) ⇒ absent(P)`.
2. **Stratified NAF:** requires a dependency stratification (none exists).
3. **Branch-relative epistemic:** `KB_C ⊭ P` where `C` is the commitment
   environment — `absent(P)` is a *query over the current saturated
   world/environment*, not a ground logical atom.

The review judges (3) closest to Ein's actual behaviour; that distinction
(query vs atom) is the important one.

## T1.21.4.1 — Investigation + report → `reports/r4_absent_semantics.md`

Read-only. Deliver:

1. **Implementation inventory** — every site where `absent` semantics is
   *decided*: the matcher's `AbsentGuard` (match-time evaluation), the
   saturator's fire-time re-check + semi-naive trigger handling, the
   hypothesis loop / `try_commitment_set` (what world the guard queries in a
   fork), contradiction detection interplay, dumps/trace rendering of
   absent-derived firings. `file:line` each.
2. **Behavioural characterisation** — run the existing NAF fixtures
   (`examples/features/`, `examples/saturation/`, tests) against the three
   candidate semantics and say which one the engine *actually* implements,
   with any divergences or underspecified corners: `absent` under nested
   `(and …)`, re-derivation after a later fact arrives (non-monotone
   retraction? or fire-time-only?), `absent` in root vs deep branch,
   interaction with `forall`/completeness checks if present.
3. **Formal statement draft** — the `KB_C ⊭ P` definition with: the world
   `W` the query runs against (root + commitment + saturation-so-far?),
   when it is (re)evaluated, and the theorems the engine relies on
   (e.g. "alive-branch facts may not be root-merged" as a corollary).
4. **Doc placement recommendation** — `docs/kernel/inference/` vs
   `docs/kernel/ir/03-ein-lang/` (surface semantics), and the cross-link
   set (glossary, inference README, S1.21.2's rewritten section).

## T1.21.4.2 — Improvement

Execute the report. Expected shape:

- Write `docs/kernel/inference/absent_semantics.md` (or the report's chosen
  home): the formal definition, the evaluation-time rules, the corollaries
  (no root-merge; positive provenance insufficient; deletion-based MUS
  unsound; fire-time re-eval required), what is explicitly *not* provided
  (stratification checking), and worked micro-examples.
- Cross-link: `docs/kernel/inference/README.md`, `docs/kernel/glossary.md`,
  `docs/kernel/README.md` index, `min_core.py` + `solver.py` docstrings
  pointing at the doc instead of re-arguing locally.
- **Pin with tests**: one test per documented edge case from the report's
  §2 (naming the doc section it pins), so the semantics doc is executable
  law, not prose.
- Gate: `./run_tests.sh` + `ruff check .` green; behaviour unchanged (this
  stage documents + pins; any *divergence* found between engine and chosen
  semantics is reported to the phase README as a new open question, not
  silently fixed here).

## Outcome (2026-08-16)

**Shipped** (T1.21.4.2, per the report's Recommendation):

- [`docs/kernel/inference/absent_semantics.md`](../../../docs/kernel/inference/absent_semantics.md)
  — **new**, transcribed from the report §3: worlds (fork-local KB),
  the fire-time epistemic definition (`W(t) ⊭ ∃x̄.Pθ`, membership not
  derivability), evaluation points E1/E2/E3, corollaries C1–C7,
  explicit non-guarantees (no stratification / stable models /
  retraction; non-stratified outcome = priority + FIFO order), the P1–P8
  probes as worked micro-examples, and the D3/D5 divergences.
- Stale `01_grammar.md` "NAF evaluation timing (known limitation)"
  section (enqueue-time-only, "parked for P1.5a") **replaced** with a
  short fire-time pointer to the new doc; the `forall`/`open`
  "parser sugar" wording corrected to `std.macro` load-time macros (D1).
- Cross-links added: inference README (layout tree + §NAF normative
  banner + the retired-unconditional section now cites §C1/§C2 anchors),
  `architecture_and_algorithms.md` §O3, `docs/kernel/README.md` index,
  `glossary.md` (new **World** and **Absent / NAF** entries),
  `06_reserved_names.md` (quick-ref + `absent` row + macro-sugar note),
  and docstrings in `match.absents_still_pass` (C4 + D5 note),
  `monotonic/solver.py` (C1/C2), `frontier.py` (C3).
- [`ein.py/tests/inference/test_absent_semantics.py`](../../../ein.py/tests/inference/test_absent_semantics.py)
  — **new**, the 8 pinning tests from report §4, each naming its doc
  section: 7 assert current behaviour (P1–P7), P8 is
  `xfail(strict=True)` on the D5 or-disjunct gap.
- Phase README gained
  [§Divergences surfaced by investigation](README.md#divergences-surfaced-by-investigation-2026-08-16):
  D3 (lookahead world mismatch, recorded) and D5 (confirmed unsound
  or-disjunct firing, **promote-to-fix candidate** with the 3-line
  `absents_still_pass` → `extra_match_plans` fix noted).

**Deviations from the report:** none of substance. `min_core.py` was
renamed to `frontier.py` by S1.21.3 before this task ran — the C3
docstring cite landed there. Gate: full `./run_tests.sh` + `ruff check .`
green; engine behaviour unchanged.

**Follow-ups:** promote D5 to an engine fix (flips the strict xfail;
retire the doc's D5 paragraph + the `absents_still_pass` gap note with
it); decide D3's world discipline for lookahead (unify the probe world
or document the under-approximation away); S1.21.6's seam doc should
cite the doc's §Worlds anchor.

# S1.21.3 — `minimal_unsat_core`: rename, or make minimality real

**Phase:** P1.21 (review response). **Priority: P1** — API/doc promises a
stronger property than the implementation guarantees.
**Review point:** [REVIEW_M1-01 §3](../REVIEW_M1-01.md).
**Depends on:** nothing (investigation read-only; improvement gated on
[`reports/r3_min_core.md`](reports/r3_min_core.md)).

## The claim

[`min_core.py`](../../../ein.py/src/ein/inference/min_core.py) documents
itself honestly ("smallest single-contradiction source frontier",
sound-by-construction via one witness's provenance leaves, deliberately
*not* the deletion-based MUS minimiser because that is NAF-unsound). But:

- It is **not** a subset-minimal MUS
  (`C unsat ∧ ∀c∈C: C∖{c} sat`) — and the top-level
  [`README.md:19`](../../../README.md) promises "minimal unsat core";
  [`reserved_engine_strings.md:46`](../../../docs/kernel/inference/reserved_engine_strings.md)
  repeats it.
- Stronger: even "smallest frontier **over all derivations**" is not
  guaranteed, because the KB stores **one `Fact` per `(relation, args)`**
  and a re-derivation returns the existing object without recording an
  alternative justification. If `A,B → X` fired first, the shorter
  `C → X` proof never enters `X`'s provenance, so the reported explanation
  depends on firing order (`{A,B}` reported while `{C}` exists).

Review options: (a) rename — `smallest_contradiction_frontier()` — and say
exactly what it is; (b) extend provenance to an OR/AND proof DAG
(`Fact X ├─ justification 1: A∧B └─ justification 2: C`) and then define/
search real minimality.

## T1.21.3.1 — Investigation + report → `reports/r3_min_core.md`

Read-only. Deliver:

1. **Verify single-justification provenance** at the store level
   (`kb/store.py` `add_and_index_fact` / `add_fact` dedup path;
   `kb/provenance.py` model): confirm alternative derivations are dropped,
   with `file:line`. Construct (on paper or as a fixture sketch) an
   order-dependent explanation instance.
2. **Promise census** — every place the *words* "minimal unsat core" (or
   `minimal_unsat_core` the symbol) appear: `README.md`,
   `docs/kernel/inference/README.md:73`, `reserved_engine_strings.md`,
   `python_impl.md:60`, CLI/trace output strings, tests, phase docs.
3. **Weigh the options**: (a) rename + precise contract (cheap, honest
   now); (b) multi-justification provenance + true minimal-explanation
   search (cost: provenance model change on the hot `add_and_index_fact`
   path — estimate; interacts with S1.21.1 canonical identity and P1.8a's
   measured hot paths); (c) staged — rename now, park (b) as a
   P1.9/follow-up entry. Recommend one; the review's floor is: the name
   must stop over-promising.
4. **Exact new contract wording** for the chosen path (docstring + README
   line + reserved-strings row).

## T1.21.3.2 — Improvement

Execute the report. Expected shape (rename path shown; adjust if (b)):

- Rename `minimal_unsat_core` → `smallest_contradiction_frontier` (module
  name too, or keep `min_core.py` with the new symbol — per report), update
  `__all__`, callers, tests; grep-zero stale references in `src/`.
- Rewrite the promise sites: `README.md:19`, kernel-doc rows, reserved
  engine strings — state "smallest single-witness source frontier
  (provenance-based, NAF-safe); not a subset-minimal MUS" in one canonical
  phrasing reused everywhere.
- If the report chose (c): add the multi-justification OR/AND proof-DAG
  follow-up as an explicit P1.9 catalog row (or a new stage file here),
  cross-linked from `min_core.py`'s docstring.
- Gate: `./run_tests.sh` + `ruff check .` green; acceptance unchanged
  (`contradictions` mode output strings may change only per the approved
  wording).

## Outcome (2026-08-16)

**T1.21.3.2 done** — staged path (c) from
[`reports/r3_min_core.md`](reports/r3_min_core.md) executed:

- **Rename shipped** (git mv, history kept):
  `ein.py/src/ein/inference/min_core.py` →
  [`frontier.py`](../../../ein.py/src/ein/inference/frontier.py),
  `minimal_unsat_core` → `smallest_contradiction_frontier` (+ `__all__`);
  `tests/inference/test_min_core.py` →
  [`test_frontier.py`](../../../ein.py/tests/inference/test_frontier.py)
  (the symbol's sole importer). Module/function docstrings now carry the
  report's §4 canonical contract verbatim (smallest **recorded**
  single-witness source frontier; NAF-safe; **not** a subset-minimal MUS;
  first-derivation-wins order dependence) and cross-link E25.
- **Order-dependence pinned**:
  `test_frontier.py::test_result_depends_on_recorded_derivation_order` —
  the report's §E3 two-derivation fixture, priorities flipped between runs;
  asserts both results sound (⊆ union core), `{C, Y}` vs `{A, B, Y}`, and
  that they differ. A future E25-style fix flips this test deliberately.
  The three pre-existing tests carried over under the new names.
- **Promise sites rewritten** with the §4 wording:
  [`README.md`](../../../README.md) `k = 0` bullet,
  [`inference/README.md`](../../../docs/kernel/inference/README.md)
  contradiction-detection row,
  [`reserved_engine_strings.md`](../../../docs/kernel/inference/reserved_engine_strings.md)
  `k = 0` shape cell,
  [`python_impl.md`](../../../docs/kernel/inference/python_impl.md) module row,
  [`architecture_and_algorithms.md`](../../../docs/kernel/inference/architecture_and_algorithms.md)
  line-29 bullet + §O6 gap paragraph (now cites the new name, the NAF-unsound
  deletion-MUS finding, and E25),
  [`glossary.md`](../../../docs/kernel/glossary.md) § Unsat core,
  [`02_store.md`](../../../docs/kernel/ir/02-data-model/02_store.md) §7.2,
  `store.py::unsat_core` docstring ("minimal" → "per the recorded
  derivations"),
  [`plans/followups/f3`](../../../plans/followups/f3_three_task_classes_first_class.md).
  CLI/trace runtime strings untouched (already honest — plain "unsat core").
- **Proof DAG parked as P1.9 E25** *(rehomed 2026-08-17 as in-phase
  follow-up stage [S1.21.7](s1.21.7_multi_justification_provenance.md) —
  user rule: new work is never scheduled into previous phases)* — record
  at the dedup seam is cheap, consumers + ATMS-label search are the cost;
  companion follow-up = wiring the frontier into the contradiction answer
  (E19's never-wired use-site, acceptance-visible → out of scope here).
  **Both executed 2026-08-17** under S1.21.7, which also corrected two claims
  this stage passed on from R3 §3(b): the cheap seam is `Saturator._apply`,
  not `store.add_and_index_fact` (which sees 8 of ~194k re-derivations), and
  the "saturate is ~1 % of a solve" runtime argument misread a self-time
  table. Recording measures +2.5 %.
  [E19](../p1.9_hypothesis_loop_followups/s1.9.e19_unsat_core_min.md) got a
  § Renamed addendum.
- **Deviation** (grep-vs-history reconciliation): the closeout grep excludes
  only `plans/ideas`, `plans/m3_smt_integration`, `docs/lib`, REVIEW_M1-01
  and P1.21's own record, so the *literal* symbol was also lifted out of the
  P1.9 historical files (E19 stage + README row) — the history is kept in
  prose/renamed form there, not as the old identifier. Both greps
  (`minimal_unsat_core`, "minimal unsat core") are zero outside the
  exclusions.
- Gate green: full `./run_tests.sh` + `ruff check .`; no runtime output
  changed (the renamed function still has no production caller).

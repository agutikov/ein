; S1.5a.1 — NAF semantic re-architecture. The saturator's
; AbsentGuard evaluates its sub-plan against the KB state at
; first-enqueue time, not at fire time; for NAF premises that
; depend on derived facts (e.g., (absent (?S ?h_o ?h1)) against
; a next-to relation populated by (symmetric) + (includes)) this
; races: between first derivation step and last, the NAF sees a
; partial KB and passes spuriously. Fix path-dependent on the
; option chosen (see § Approach options in the parent README).

# S1.5a.1 — NAF semantic re-architecture

**Phase:** P1.5a (zebra solution + saturator/NAF gaps).
**Estimate:** 2-5 days, depending on path (engine fix vs gating
vs ein-side workaround).
**Status:** **shipped** 2026-05-24. T1.5a.1.1 engine fix landed
in [`match.absents_still_pass`](../../../ein.py/src/ein_bot/inference/match.py)
+ [`saturator._apply`](../../../ein.py/src/ein_bot/inference/saturator.py)
(`Saturator.naf_dropped` counter), with [5 unit tests in
`test_saturator_naf.py`](../../../ein.py/tests/inference/test_saturator_naf.py)
covering the race and its absence; 623-test regression suite
green; semantics + termination documented in
[`docs/kernel/inference/README.md`](../../../docs/kernel/inference/README.md#naf-semantics--fire-time-re-evaluation-s15a1).
T1.5a.1.2 (static NAF dependency map) **spun out** to
[S1.5a.8](s1.5a.8_naf_dependency_map.md) — pure observability,
independent of the runtime fix that already shipped. T1.5a.1.3
(ein-side workaround docs) **closed as obsolete** — it was the
shortcut path that T1.5a.1.1's durable fix supersedes; no
puzzle now needs the "pre-declare derived facts" pattern. Zebra2
root saturation has `naf_dropped == 0` — the existing priority
ordering (`(includes right-of next-to)` + `(symmetric next-to)`
at 100 drains before priority-200 NAFs) already prevents the
race for that specific shape; the fix is the engine-level
guarantee future rule sets and hypothesis branches can rely on.
T1.5a.1.1 was the critical-path piece for P1.5a; zebra2's
unique solve still requires [S1.5a.2](s1.5a.2_hypgen_pre_pruning_recovery.md)
to compose.
**Depends on:** [S1.5.8c](../p1.5_hypothesis_loop/s1.5.8c_final_decision.md)
shipped (3 kernel deltas, including the `AbsentGuard` rename);
the saturator's `_apply` flow is otherwise unchanged.
**Blocks:** [S1.5a.2](s1.5a.2_hypgen_pre_pruning_recovery.md)
(pre-pruning recovery composes with correct NAF), [S1.5a.13](s1.5a.13_acceptance_zebra2_solves.md)
(zebra2 cannot solve uniquely until the race is closed or
worked around).

## Context

Today's `AbsentGuard` (post-S1.5.8c-3) compiles its sub-plan
once at rule-compile time and evaluates the sub-plan against
the KB state at **first-enqueue time**. The firing then commits
regardless of later KB state — by the time the firing is
dequeued, the AbsentGuard's "I saw no fact matching X" snapshot
may be stale.

Concretely (from the parent README):

- `cross-attr-spatial-fwd` for condition (12) `(Kools next-to
  Horse)` fires deterministically for non-corner Kools houses,
  inventing a unique-neighbour conclusion when the puzzle's
  semantics says the case is disjunctive.
- `disjunctive-prune` (intended to derive negatives for
  non-adjacent houses) has the same exposure — same NAF shape.

The current workaround in `examples/zebra2.ein` is to declare
next-to facts explicitly in `(facts)` (8 facts replacing 4
`right-of` + `symmetric`/`includes` derivation). Dodges the
race for *this* relation; doesn't fix the underlying semantics.

## Approach options

Reproduced + sharpened from the parent README:

### (1) Engine: re-evaluate NAF at fire time

When a queued firing dequeues, re-run its match (or just its
NAF sub-plans) against the current KB. If NAF now fails, drop
the firing. ~15-30 LOC in `inference/saturator.py`'s `_apply`.

- **Pro:** most principled. Fixes every NAF-on-derived-fact
  case at once, not just the spatial ones in zebra2.
- **Con:** changes the engine's enqueue ⇒ commit invariant.
  Need to think about whether dropped firings interact with the
  fixpoint termination check.

This is the long-term answer. T1.5a.1.1 explores it.

### (2) Per-rule "wait for closure" gating — *not pursued*

Defer rules whose NAF depends on derived facts until those
facts are saturated. Needs a "phase marker" mechanism the
engine doesn't have today and requires a static analysis of
which NAFs depend on which derived facts. The static-analysis
half ended up worth doing on its own merits as a load-time
warning; spun out to [S1.5a.8](s1.5a.8_naf_dependency_map.md)
where the gating motivation is dropped.

### (3) Ein-side: pre-declare derived facts — *obsolete*

Sidestep the race by manually enumerating the derived facts in
`(facts)` or `(ontology)`. Per-puzzle workaround; doesn't
generalise. Closed 2026-05-24: the durable fix in (1) shipped,
so no puzzle now needs the workaround. Kept here as a historical
note for the design space.

## Tasks

### Task T1.5a.1.1 — Re-evaluate NAF at fire time

- **a.** Identify the dequeue path in `saturator.py` `_apply`;
  insert a re-evaluation step that re-runs the firing's
  `AbsentGuard` sub-plans against current KB.
- **b.** If re-eval fails, drop the firing; record metric
  `naf_re_eval_dropped` for visibility.
- **c.** Re-run zebra2 with derived next-to (the
  `right-of`+`symmetric`+`includes` chain rather than 8 explicit
  next-to facts); verify that `cross-attr-spatial-fwd` no longer
  fires the spurious unique-neighbour conclusions.
- **d.** Termination invariant: confirm a dropped firing doesn't
  prevent saturation from reaching fixpoint (re-enqueue if any
  watched fact changes? Document the invariant either way.).
- **e.** Tests: at least 3 saturation tests that exercise NAF
  over derived facts and pass under fire-time re-eval, fail
  under enqueue-time eval. Plus a regression: zebra2 derived
  next-to solves uniquely.

### Task T1.5a.1.2 — Static NAF dependency map — *spun out*

Moved to [S1.5a.8 (static NAF dependency map)](s1.5a.8_naf_dependency_map.md)
2026-05-24. Pure observability with its own acceptance bar;
doesn't compose with T1.5a.1.1's runtime correctness.

### Task T1.5a.1.3 — Ein-side workaround pattern docs — *obsolete*

Closed 2026-05-24 — T1.5a.1.1 (engine re-eval) shipped, so the
"pre-declare derived facts" workaround is no longer required by
any puzzle. The original motivation was an M1-acceptance
shortcut while the durable fix was being designed; with the
durable fix already landed the shortcut is moot.

## Acceptance

- ✅ T1.5a.1.1 ships: `cross-attr-spatial-fwd` and
  `disjunctive-prune` fire only on the genuine — not spurious —
  neighbour cases under zebra2 with derived next-to
  (`(includes right-of next-to)` + `(symmetric next-to)`). The
  unique-solve gate moved to [S1.5a.13](s1.5a.13_acceptance_zebra2_solves.md)
  — it composes T1.5a.1.1 with S1.5a.2 (hypgen pre-pruning) and
  isn't determined by the NAF fix alone.
- ✅ [`pytest tests/inference/test_saturator_naf.py`](../../../ein.py/tests/inference/test_saturator_naf.py)
  green: 5 tests covering NAF preserved when derived fact never
  arrives, NAF dropped when derived fact arrives between
  enqueue and fire, the `naf_dropped` counter, per-binding NAF
  preservation, and a zebra-shaped derivation chain.
- ✅ No regression in existing pytest suite: 623 passing
  post-ship (was 620 pre-fix + 5 new − 2 deleted obsolete
  zebra2-structure assertions in `tests/kb/test_store.py` after
  the dead-weight `(asymmetric is-a)` + `(sibling-exclusive is-a is-a)`
  removal).

## Open questions

- **Q-S1.5a.1.A — Termination invariant under fire-time NAF
  re-eval.** ~~Does dropping a firing late in the cycle still
  guarantee saturation termination?~~ Resolved 2026-05-24 with
  the T1.5a.1.1 ship: within a single `saturate()` run the fact
  base grows monotonically (no retractions), so once an
  AbsentGuard fails at fire time the watched fact stays in the
  KB; the binding sits in `engine._fired` and the saturator does
  not re-enqueue it. A's *false* derivation is removed from the
  fixpoint candidate set without re-entering — termination is
  preserved. C, if it needed A's conclusion, would also have been
  predicated on the same now-violated NAF and dropped on its own
  fire-time check (or its premise simply never materialised).
  Written up in
  [`docs/kernel/inference/README.md`](../../../docs/kernel/inference/README.md#naf-semantics--fire-time-re-evaluation-s15a1).
  Hypothesis-branched saturation runs over a fresh
  `_seen`/`_queue` (per `kb.fork()`), so dropped-firing state is
  scoped to one branch.
- **Q-S1.5a.1.B — Per-NAF caching.** Re-evaluating every
  firing's NAF at fire time is O(NAFs × firings). Worth caching
  the NAF result per `(rule, binding)` and invalidating on
  watched-fact arrival? Composes with [P1.9 E8 watched-fact
  rule applicability](../p1.9_hypothesis_loop_followups/README.md).

## Cross-links

- [P1.5a README — Approach options for (a) NAF race](README.md#approach-options-for-a-naf-race)
  — the design space this stage picks from.
- [`inference/saturator.py`](../../../ein.py/src/ein_bot/inference/saturator.py)
  — the engine module this stage edits.
- [S1.5.8c](../p1.5_hypothesis_loop/s1.5.8c_final_decision.md)
  — the AbsentGuard rename + `(absent …)` head registration this
  stage builds on.
- [P1.9 E8 watched-fact rule applicability](../p1.9_hypothesis_loop_followups/README.md)
  — composes with the per-NAF caching question above.
- [docs/kernel/inference/](../../../docs/kernel/inference/) —
  T1.5a.1.3 documents the workaround pattern here.

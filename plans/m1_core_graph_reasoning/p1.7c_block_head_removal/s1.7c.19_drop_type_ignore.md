# S1.7c.19 — Remove the two `type: ignore[arg-type]`

**Phase:** P1.7c, Track B.
**Estimate:** ~½ hr. Trivial — typing only.
**Depends on:** P1.7b baseline. Cleanest **after**
[S1.7c.15](s1.7c.15_split_lattice_loop_state.md) (concretely-typed per-entry
state removes the underlying non-narrowing).
**Finding:** [F-ENG-13](../p1.7b_review_and_refactor/findings.md#eng--monotonic-search-engine-inferencemonotonic).

## Goal

`solver.py:762` and `:1067` carry `entry=...  # type: ignore[arg-type]`,
papering over a `Literal` that fails to narrow when guarded through an
`in`-tuple membership test.

## Tasks

- Narrow with an explicit `==` chain (or assign a typed local) so the
  checker accepts the `entry` argument without the suppression.
- Delete both `# type: ignore[arg-type]` comments.

## Acceptance

- Zero `type: ignore` in `solver.py`.
- Type-checker clean; `ruff check .` green; full suite green.

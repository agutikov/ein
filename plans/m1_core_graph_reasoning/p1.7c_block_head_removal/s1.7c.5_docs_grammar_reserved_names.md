# S1.7c.5 — Docs: grammar + reserved-names + examples reflect flat forms

**Phase:** P1.7c, Track A.
**Estimate:** ~½ day.
**Depends on:** [S1.7c.2](s1.7c.2_flat_form_grammar.md) (final grammar) +
[S1.7c.4](s1.7c.4_migrate_and_drop_shim.md) (flat is the only surface).
**Resolves:** the doc-sync tail of Track A.

## Goal

Make the kernel docs describe the **flat-form** language as current — the
block heads are gone, a program is a sequence of forms classified by head.

## Tasks

- **[`03-ein-lang/01_grammar.md`](../../../docs/kernel/ir/03-ein-lang/01_grammar.md)**
  — rewrite the top-level section: drop the "7 reserved block heads"
  framing; document `start: form*` over the declarator/fact alternation +
  the **classify-by-head** rule (head ∈ closed declarator set → declarator;
  else → fact). Show the layer-attribution rule chosen in S1.7c.1.
- **[`grammar.lark` header](../../../ein.py/src/ein_bot/ir/grammar.lark)** —
  the big comment block (items 1–8) still says "Top level: exactly 7 reserved
  heads" and describes the `(ontology …)` / `(facts …)` / `(reasoning …)`
  interiors. Rewrite to the flat model; keep items 6–8 (kw-pair alternation,
  list-head restriction, meta-primitive reservation) — those are unchanged.
- **[`06_reserved_names.md`](../../../docs/kernel/ir/03-ein-lang/06_reserved_names.md)**
  — the **source of truth** for the classifier. Add `query` / `config`
  (and reserve `macro` forward) to / alongside the Declarators table, and
  state explicitly: *a top-level head in this set is a declarator; any other
  head is a fact* (the closed-set contract from S1.7c.2's Q3). Note `trace`
  as the engine-emitted sibling.
- **[`03_examples.md`](../../../docs/kernel/ir/03-ein-lang/03_examples.md)**
  + [`03-ein-lang/README.md`](../../../docs/kernel/ir/03-ein-lang/README.md)
  — re-snippet any wrapped example to flat; update prose that says "inside
  `(facts …)`".
- **`docs/ir.md`** redirect + the [examples/README.md](../../../examples/README.md)
  M1 target trace — sweep for wrapper mentions.

## Acceptance

- Grep across `docs/kernel/` + the grammar header for `(ontology`/`(facts`/
  `(reasoning`/`(rules` wrapper references → only **historical** mentions
  remain (clearly marked "pre-P1.7c"), none described as current syntax.
- `06_reserved_names.md` lists the full closed top-level declarator set and
  the "else → fact" contract; the parser/loader key on that same set.
- Docs cross-references resolve (the [reserved-names boundary](../../../docs/kernel/inference/reserved_engine_strings.md)
  pairing stays intact).

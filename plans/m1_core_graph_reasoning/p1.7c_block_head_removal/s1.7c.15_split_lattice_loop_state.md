# S1.7c.15 — Split `_LatticeLoopState` into per-entry accumulators

> **Status: DONE — split ASSESSED & REJECTED; kept one documented class
> (2026-06-03).** Attempted via a 5-agent design panel (3-way+Protocol /
> 2-way+base / keep-and-document / ctx-typed); the split verdict was
> unanimous **avoid** (conf 4–5). The fields don't partition: the entry
> field-sets form a Venn lattice with non-empty *pairwise* intersections
> (`dead_commitments`={solve,contradictions}, `kb_index`={gaps,
> contradictions}, `alive_at_end_tuple`=all-three) but an **empty triple
> intersection** — and a class tree can't express a Venn lattice, so any
> split (a) duplicates `dead_commitments` + `kb_index` across subclasses,
> (b) forces the cross-entry mutators (`_handle_dead`, `_record_setnode`,
> `_finalise_lattice_verdict`) onto a Union/Protocol observationally
> identical to today's class, and (c) replaces the structurally-impossible
> mis-route with a *possible* one against the 101/215/0 gate (the
> silent-verdict-corruption risk this stage's own estimate flags). It is
> net **+30–50 LOC, weaker safety**, and `.19` (the only stated motivation)
> already shipped independently via `==`-narrowing. So — like
> [S1.7c.22](s1.7c.22_typed_index_wrappers.md) — the right move is the
> minimal one: keep ONE class, replace the stale 30-line caveat docstring
> (which wrongly said "for `entry in (gaps, contradictions)`") with an
> accurate field→entry table. That satisfies the acceptance ("no field read
> by zero entries" — the table shows all 8 read by ≥1 entry) at zero
> byte-risk (docstring-only; field declarations untouched, full Phase-1
> suite green).

**Phase:** P1.7c, Track B.
**Estimate:** ~3–4 hr. **Medium–high risk** — it is the loop's mutable state;
a field aliased to the wrong entry is a P1.7a-class bug. Gate hard.
**Depends on:** P1.7b baseline. Unblocks [S1.7c.19](s1.7c.19_drop_type_ignore.md)
(the `type: ignore` vanish once each entry's state is concretely typed).
**Finding:** [F-ENG-8](../p1.7b_review_and_refactor/findings.md#eng--monotonic-search-engine-inferencemonotonic).

## Goal

`_LatticeLoopState` (`solver.py:155`) mixes solve-only fields
(`solution_nodes`, `truncated`) with lattice-only fields (`solutions`,
`kb_index`, `state_hash_merges`, `root_was_solved`); **no single run uses all
of them**, so the type carries a 30-line per-field-per-entry caveat
docstring describing which fields are live for which entry.

## Tasks

- Split into per-entry accumulator dataclasses (or a shared base + a
  `_SolveState` / `_LatticeState` pair); each phase function receives the
  accumulator it actually uses.
- Delete the per-field caveat docstring — the types now say it.

## Acceptance

- No field is read by zero entries.
- **Identical enterings + verdicts** across the 3-variant acceptance gate
  (zebra2 / minus-15 / invariant) — this is the hard net.
- Full suite green.

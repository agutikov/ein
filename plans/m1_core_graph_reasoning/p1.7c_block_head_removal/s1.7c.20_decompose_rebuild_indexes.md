# S1.7c.20 — Decompose `rebuild_indexes`

**Phase:** P1.7c, Track B.
**Estimate:** ~3 hr. **Medium risk** — load + snapshot path; the index state
must come out bit-identical. Gate on fork-parity + shuffle-invariance tests.
**Depends on:** P1.7b baseline.
**Finding:** [F-KB-2](../p1.7b_review_and_refactor/findings.md#kb--data-model-kbpy).

## Goal

`rebuild_indexes` (`store.py:300`, ~164 ln, 61 branches) builds **8 indexes
inline** and walks `self.facts` **twice** (`:351` and `:382` collect nearly
the same head/arg groupings) — the longest non-flagship function left in the
package after P1.7b.

## Tasks

- Split into per-index builder helpers (one per `_*_by_*` index).
- **Fuse the two fact walks** into a single pass that feeds all fact-derived
  builders.
- Early-skip the always-empty `_rules_by_type` block with the single parked
  pointer (the [F-KB-11](../p1.7b_review_and_refactor/findings.md#kb--data-model-kbpy)
  seam — keep it, just stop it reading as a mystery).

## Acceptance

- Index state byte-identical to baseline after load **and** after `snapshot`
  (fork-parity + shuffle-invariance tests).
- `rebuild_indexes` (and each helper) < ~50 ln.
- Full suite green; 3-variant gate byte-identical.

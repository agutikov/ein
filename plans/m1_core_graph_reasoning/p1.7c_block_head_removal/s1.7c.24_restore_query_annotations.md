# S1.7c.24 — Restore `Query` field type annotations

**Phase:** P1.7c, Track B.
**Estimate:** ~1 hr. Low risk — typing only.
**Depends on:** P1.7b baseline.
**Finding:** [F-KB-13](../p1.7b_review_and_refactor/findings.md#kb--data-model-kbpy).

## Goal

`Query.kw_pairs` / `config` / `alive` / `consume_stats`
(`store.py:98,133,141,149`) are typed `Any` to dodge the `inference → kb`
import cycle — but the module already imports under `TYPE_CHECKING`, so the
checker is losing coverage on the **solver's most-touched fields** for no real
reason.

## Tasks

- Add proper annotations using `TYPE_CHECKING` forward refs (string
  annotations / `from __future__ import annotations`) for the four fields.
- Confirm no runtime import cycle is introduced (the refs stay
  type-check-only).

## Acceptance

- The four fields are concretely typed; no `Any`.
- Type-checker clean (and now actually checking these fields); no runtime
  cycle; full suite green.

# S1.7c.27 — Split `_build_parser` + add `_load_kb_or_exit`

**Phase:** P1.7c, Track B.
**Estimate:** ~2 hr. Low–medium risk — CLI surface must not change.
**Depends on:** P1.7b baseline. **Internal-only** — this does *not* pre-empt
the [P1.11](../p1.11_package_restructure/README.md) cli folder move.
**Finding:** [F-RTC-2](../p1.7b_review_and_refactor/findings.md#rtc--render--trace--cli--ir).

## Goal

`_build_parser` (`cli.py:270`, ~154 ln) assembles **every** subparser inline
(the `rule_mode_opts` dict at `:354` shows the smell was half-recognised); the
`_cmd_ir_*` handlers repeat the parse-or-bail prelude (a `_parse_or_exit`
helper exists but three sites predate it), and the **KB-load-or-bail block is
copy-pasted verbatim 3×**.

## Tasks

- Split `_build_parser` into one `_add_<cmd>_parser(subparsers)` per
  subcommand.
- Add `_load_kb_or_exit(path) -> KnowledgeBase`; route the three load sites
  (and the three `_parse_or_exit` predaters) through the helpers.

## Acceptance

- `ein --help` and **every** subcommand's `--help` byte-identical to baseline.
- CLI tests green; full suite green; no change to argument names/defaults.

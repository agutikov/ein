# S1.7c.30 — `linearize` dispatch table

**Phase:** P1.7c, Track B.
**Estimate:** ~2 hr. Low–medium risk — gate on all three verdict kinds.
**Depends on:** P1.7b baseline.
**Finding:** [F-RTC-5](../p1.7b_review_and_refactor/findings.md#rtc--render--trace--cli--ir).

## Goal

`linearize` (`trace/linearize.py:118`, ~96 ln) dispatches on
`(verdict-type × proof?)` with **five near-identical hand-built `Trace(...)`**
and the `solution_dot` / `full_kb_dot` guarded-render lines spelled out 3×.

## Tasks

- Convert the dispatch to a `{VerdictType: _from_<kind>}` table.
- Extract a `_trace_diagrams(kb, …)` helper for the guarded diagram render.

## Acceptance

- Linearised trace + embedded diagrams byte-identical to baseline across
  Solution / Ambiguity / Contradiction (the three gate fixtures).
- Trace tests green; full suite green.

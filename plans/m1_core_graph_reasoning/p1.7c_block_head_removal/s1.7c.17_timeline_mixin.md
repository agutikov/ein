# S1.7c.17 — Extract a `_TimelineMixin` for the dumpers

**Phase:** P1.7c, Track B.
**Estimate:** ~1 hr. Low risk — dump-only surface.
**Depends on:** P1.7b baseline.
**Finding:** [F-ENG-11](../p1.7b_review_and_refactor/findings.md#eng--monotonic-search-engine-inferencemonotonic).

## Goal

`MonotonicDumper._emit_timeline` / `summary` / `close`
(`state_dump.py:342`+) are duplicated **verbatim** in `LatticeDumper`
(`:875`+) — ~50 lines that differ only by a `default=str` in the JSON
serialisation.

## Tasks

- Extract a `_TimelineMixin` carrying `_emit_timeline` / `summary` / `close`,
  parameterising the `default=` serialiser (class attr or ctor arg).
- Mix it into both `MonotonicDumper` and `LatticeDumper`; delete the copies.

## Acceptance

- Dumped timeline JSON is **byte-identical** to baseline for both dumpers
  (trace/state-dump tests).
- Full suite green.

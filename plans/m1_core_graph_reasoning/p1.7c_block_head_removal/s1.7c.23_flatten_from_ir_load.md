# S1.7c.23 — Flatten `from_ir.load` nesting (depth 8)

> **Status: SUPERSEDED by Track A (2026-06-03).** Track A's flat-form
> landing ([S1.7c.3](s1.7c.3_loader_flat_routing.md), commit `03ce4f5`)
> rewrote `load` from scratch: the **8-way block dispatch this stage
> targeted no longer exists** — there is no `_ingest_ontology` /
> `_ingest_facts` / `_ingest_reasoning`, and no re-derived
> "is-it-an-SForm" preamble. `load` is now a flat `for form in forms`
> head-classification loop (relation / rule / hrule / query / config /
> trace / else→fact) feeding the three classic passes; the per-`examples`
> identical-KB acceptance is met (and gated every stage). The function's
> nesting reads as depth **6** only because the metric counts the 6-arm
> `elif` head-classification ladder as nesting — that ladder *is* the
> intended flat dispatch, not the deep block structure F-KB-7 flagged, and
> contorting it into a dict-dispatch to chase `<5` would hurt readability.
> **Residual (optional, NOT this stage):** a few stale comments in
> `from_ir.py` (the docstring + `_ingest_rules` still mention "deprecated
> block wrappers" / the dropped back-compat shim) — a trivial comment-sweep,
> not a refactor.

**Phase:** P1.7c, Track B.
**Estimate:** ~2–3 hr. Medium risk — the loader; gate on every
`examples/*.ein` loading identically.
**Depends on:** P1.7b baseline. **Coordinate with Track A** — the block-head
removal ([S1.7c.2 / .3](README.md)) rewrites this same dispatch; sequence
after it or fold the two together to avoid touching `load` twice.
**Finding:** [F-KB-7](../p1.7b_review_and_refactor/findings.md#kb--data-model-kbpy).

## Goal

`load` (`from_ir.py:361`, nesting depth **8**) runs an 8-way head dispatch
over parallel block lists; `_ingest_ontology` / `_ingest_rules` /
`_ingest_facts` each re-derive the "is-it-an-SForm-what's-the-head" preamble.

## Tasks

- Extract `_dispatch_blocks` (the head→handler routing), `_form_head` (the
  shared preamble), and `_build_rule`.
- Collapse `load` to a flat dispatch loop (depth < 5).

## Acceptance

- Every `examples/*.ein` + the inline fixtures load to an **identical** KB
  (fact/rule/relation counts + index state) vs baseline.
- Loader tests green; depth < 5; full suite green.

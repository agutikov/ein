# S1.7c.14 — Collapse the duplicated unsat-core synthesis

**Phase:** P1.7c, Track B.
**Estimate:** ~2 hr. Medium risk — unsat cores are the contradiction
verdict's payload; gate on the bad fixture's exact nogood set.
**Depends on:** P1.7b baseline.
**Finding:** [F-ENG-7](../p1.7b_review_and_refactor/findings.md#eng--monotonic-search-engine-inferencemonotonic).

## Goal

Unsat-core synthesis is duplicated **2×2** in `solver.py`:

- **dead-core union** — `verdict_of` (`:202`) vs `_finalise_lattice_verdict`
  (`:508`);
- **source-frontier core** — `_contradiction` (`:420`) vs `_root_dead`
  (`:695`).

Two near-identical implementations of each → they can drift, and a fix to
one core path silently misses the other.

## Tasks

- Extract `_union_dead_cores(...)` and `_source_frontier_core(...)`.
- Route all four sites through the two helpers; delete the inline copies.

## Acceptance

- The `bad` fixture still yields **Contradiction k=0 with the identical
  nogood / unsat-core set** (byte-compare the proof record).
- Full suite green; 3-variant gate byte-identical.

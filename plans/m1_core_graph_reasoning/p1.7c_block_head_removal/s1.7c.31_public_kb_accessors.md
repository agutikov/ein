# S1.7c.31 — Public KB / verdict accessors (`facts_for`, `query_value`)

> **Status: DONE (2026-06-03).** Both halves resolved:
> - `query_value` — promoted `_query_value` → public `query_value` in
>   `verdict.py` and updated the `trace/answer.py` import/call (commit
>   `9bb1418`).
> - `facts_for` — **moot.** The private reach this would replace
>   (`_nation_at` indexing `kb._facts_by_relation` from `trace/answer.py`)
>   no longer exists: the answer path was refactored to `query_value` /
>   `goal_bindings` / `_who_by_house`. A grep of `trace/` finds **no**
>   `kb._…` access and **no** underscore-private import (the only
>   underscore-bearing imports are public functions like `fact_label` /
>   `render_why`). The stage's acceptance ("no private-attribute /
>   underscore-import access from `trace/`") therefore holds without adding
>   `facts_for` — adding it now would be YAGNI. (If F-KB-3's hot-path
>   callers ever want a public `facts_for`, raise it there, not here.)

**Phase:** P1.7c, Track B.
**Estimate:** ~1 hr. Low risk.
**Depends on:** P1.7b baseline.
**Finding:** [F-RTC-9](../p1.7b_review_and_refactor/findings.md#rtc--render--trace--cli--ir).

## Goal

`trace/answer.py` reaches into internals: `_nation_at` indexes private
`kb._facts_by_relation` (`:21`/`:68` region) and imports the underscore-private
`_query_value` from `verdict` — a cross-module reach into two privates from
the answer path.

## Tasks

- Add a public `kb.facts_for(relation) -> tuple[Fact, ...]` accessor; route
  `_nation_at` through it (this is also the accessor F-KB-3's hot-path
  callers want).
- Promote `_query_value` → `query_value` in `verdict.py`; update `verdict`,
  `trace/answer.py`, and any other importer.

## Acceptance

- No private-attribute / underscore-import access from `trace/`.
- The answer path (`ein` answer / explanation output) unchanged; full suite
  green.

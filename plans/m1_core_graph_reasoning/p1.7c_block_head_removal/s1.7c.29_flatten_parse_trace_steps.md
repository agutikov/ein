# S1.7c.29 — Flatten `parse_trace_steps` (depth 9 — the deepest in the repo)

**Phase:** P1.7c, Track B.
**Estimate:** ~2 hr. Medium risk — gate on the trace round-trip.
**Depends on:** P1.7b baseline. Unblocks
[S1.7c.28](s1.7c.28_unify_trace_pipelines.md) (the unified producer).
**Finding:** [F-RTC-4](../p1.7b_review_and_refactor/findings.md#rtc--render--trace--cli--ir).

## Goal

`parse_trace_steps` (`trace/ast.py:120`) is **nesting depth 9 — the deepest
function in the codebase** (the `:bind` arm is its own parser nested six
deep).

## Tasks

- Extract `_parse_bindings` and `_parse_using` + a key-dispatch over the
  trace-step keywords.
- Collapse the recurring `Atom→name / scalar→value / else str` ternary
  (`:161`, also in `_sform_to_factref`) into one `_atom_or_value` helper.

## Acceptance

- Trace round-trip **byte-identical** (`tests/trace/test_render.py:147`).
- Depth < 5; full suite green.

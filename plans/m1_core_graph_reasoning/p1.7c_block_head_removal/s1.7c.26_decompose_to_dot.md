# S1.7c.26 — Decompose `to_dot`

**Phase:** P1.7c, Track B.
**Estimate:** ~2 hr. Medium risk — render output byte-identical.
**Depends on:** P1.7b baseline. Pairs with — and reads best **after** —
[S1.7c.25](s1.7c.25_shared_dot_emitter.md) (route the per-fact dispatch
through the new emitter).
**Findings:** [F-KB-10 ≡ F-RTC-6](../p1.7b_review_and_refactor/findings.md#kb--data-model-kbpy)
(the same `to_dot`, listed under both the KB and RTC views).

## Goal

`to_dot` (`kb/render.py:206`, ~130 ln) inlines six phases plus the per-fact
`is-a` / binary / hyperedge dispatch — the KB-side half of the missing shared
emitter.

## Tasks

- Extract `_resolve_nodes` (the node-collection phases) and `_emit_fact_line`
  (the per-fact dispatch).
- Route `_emit_fact_line` through the [S1.7c.25](s1.7c.25_shared_dot_emitter.md)
  `node()/edge()` API.

## Acceptance

- `to_dot` output byte-identical to baseline (render golden tests).
- `to_dot` < ~50 ln; full suite green.

; S1.10.H2 — Declare the 4-level KB representation (objects /
; facts / relations / rules). Add §9 to 03_ein_model.md (or a
; sibling doc) with the level table and the "each level
; consumes levels below" framing.

# S1.10.H2 — Ein-model: 4-level KB representation

**Phase:** P1.10 (kernel documentation), Theme H.
**Estimate:** 1-2 days.
**Status:** parked; sequential after [S1.10.H1](s1.10.h1_atom_vs_object.md)
(uses atom/object vocabulary).
**Depends on:** S1.10.H1.
**Blocks:** [S1.10.H3](s1.10.h3_self_describing_kb.md) (the
self-describing model needs the level table as its target
schema).

## Context

User direction 2026-05-24:

> *"4 levels — objects, facts, relations, rules. Idea: create
> self-describing model of KB types in IR ein lang."*

This stage adds the level table; S1.10.H3 sketches the
self-describing extension.

The proposed 4-level KB:

| level         | what lives here                                       |
|---------------|-------------------------------------------------------|
| **L0 objects**| Named atoms standing for entities (instances + types). |
| **L1 facts**  | Propositions over objects: `(R o1 o2)`.                |
| **L2 relations** | Declarations + property tags: `(relation R T1 T2)`, `(symmetric R)`, `(functional R)`. |
| **L3 rules**  | First-class rewriting machinery: `(rule NAME ... :match ... :assert ...)`. |

Each level *consumes* the levels below: rules pattern-match
facts and assert facts; relation declarations type the facts;
facts name objects.

## Tasks

### Task T1.10.H2.1 — Pick the doc location

Two options:

- **(a)** New section §9 in
  [`03_ein_model.md`](../../../docs/kernel/ir/01-ein-graph/03_ein_model.md)
  (sibling to §1-§8).
- **(b)** New sibling file `01-ein-graph/05_four_level_kb.md`.

(b) is cleaner if the content grows past ~150 lines; (a) is
simpler if it stays compact. Default: (b).

### Task T1.10.H2.2 — Write the level table

- **a.** The 4-row table.
- **b.** Per-level: what *lives* there, what *consumes* it
  from above, what *it consumes* from below.
- **c.** Examples per level (drawn from zebra2 or jack-drinks-coffee).

### Task T1.10.H2.3 — "Each level consumes lower levels"

A diagram (DOT) showing the consumption arrows:

```
L3 rules ───► L2 relations ───► L1 facts ───► L0 objects
              (declares typing                  (named atoms)
               for slot-arity)
               also fires on facts via :match
```

### Task T1.10.H2.4 — Reflexive cases

Edge case: rules that match other rules (F5 rules-as-data) or
rules that declare relations dynamically. These break the
strict-DAG consumption — document the open ground here, defer
to F5 + S1.10.H3.

### Task T1.10.H2.5 — Cross-links

- **a.** From `03_ein_model.md` §1 (the reflexive instance-of-
  instance section) — link forward to the level table.
- **b.** From [`README.md` (kernel)](../../../docs/kernel/README.md)
  reading-order — add the level table as a reference page.

## Acceptance

- The 4-level table is documented (in §9 or a new file).
- Per-level descriptions + examples.
- Consumption diagram rendered.
- Reflexive edge cases noted with forward-pointers to F5 +
  H3.

## Open questions

- **Q-S1.10.H2.A — Are types a separate level?** L0 lumps
  instances + types as "named atoms standing for entities".
  Pre-zebra2, types were a separate L-level (`type` /
  `instance` were kernel meta-primitives). Post-zebra2, both
  are `is-a`-related and live at L0. Document the unification.
- **Q-S1.10.H2.B — What's L4?** Rules-on-rules? F5 territory.
  Open ground.

## Cross-links

- [P1.10 README — Theme H](README.md#theme-h--ein-model-update-atoms-vs-objects-4-levels)
  — phase context.
- [S1.10.H1](s1.10.h1_atom_vs_object.md) — prerequisite
  vocabulary.
- [S1.10.H3](s1.10.h3_self_describing_kb.md) — the next stage
  that sketches a self-describing KB schema.
- [`docs/kernel/ir/01-ein-graph/03_ein_model.md`](../../../docs/kernel/ir/01-ein-graph/03_ein_model.md)
  — primary target doc.
- [F5 — rules as data](../../followups/f5_rules_as_data.md) —
  long-term L4 work.

# S1.22.99 — M1-plans preservation census → migrate + delete

**Phase:** P1.22. **Priority: P1**, strictly **after** S1.22.1 (the purge
edits docs whose inbound links this census inventories).
**Depends on:** T1.22.1.2 shipped. T1.22.99.2 gated on
[`reports/c4_m1_plans_deletion.md`](reports/c4_m1_plans_deletion.md).

**Renumbered from S1.22.2 → S1.22.99 (user, 2026-08-17).** This stage
deletes the folder every other P1.22 stage documents itself in, so it is
terminal by construction and must stay last however many stages get inserted
before it; `99` reserves `S1.22.2`…`S1.22.98` for those. Its report also
moved `c2` → `c4`: the `c<n>` sequence follows *execution* order, not stage
number, and `c2` / `c3` were taken by S1.22.1a and S1.22.1b.

## The ruling (user, 2026-08-16)

After the obsolete-syntax purge, **delete the M1 plans** —
`plans/m1_core_graph_reasoning/` in full (this phase's own folder
included). History lives in git; the working tree keeps only what is still
*live*.

## What is known to be live inside the folder (must be rehomed first)

- **P1.9 E-catalog (E1–E24)** — research backlog.
- **P1.21 parked follow-up stages S1.21.7 (multi-justification
  provenance) and S1.21.8 (boundary NAF)** — ex-P1.9 E25/E26, moved
  in-phase 2026-08-17; cross-linked from `frontier.py` docstrings,
  `docs/kernel/architecture.md`, `docs/kernel/inference/README.md`,
  `test_frontier.py`, and M3 Q30 — those links must be retargeted, not
  orphaned.
- **P1.7c Track B** (S1.7c.10–.32) — the deferred refactor-debt tail,
  recommended before the M1a Rust port.
- **P1.21 divergences** — D3 (lookahead world mismatch), D5 (or-disjunct
  `AbsentGuard` fire-time gap — confirmed unsound firing, promote-to-fix
  candidate), the `_helpers.py` `landed` NameError; recorded in the P1.21
  phase README which dies here.
- **M1 `open_questions.md`** still-open rows (Q26 compound/virtual node
  kinds, Q28 empty-parens semantics) + any cross-milestone question rows
  that `plans/open_questions.md` merely indexes.
- **P1.21 reports** — analyses now superseded by the docs P1.21 itself
  updated; default **die with the folder** unless the census finds a
  doc-worthy remainder (then it moves into `docs/kernel/`, not a plans
  copy).

## T1.22.99.1 — Census + report → `reports/c4_m1_plans_deletion.md`

Read-only, on the post-purge tree. Deliver:

1. **Inbound-link inventory** — every reference to
   `m1_core_graph_reasoning` outside the folder (`docs/kernel/**` (~20
   files), tests'/acceptance's plan-path comments, `README.md`,
   `plans/README.md` roadmap, `plans/open_questions.md`,
   m1a/m1b/m2/m2b/m3/followups READMEs, `utils/`), with the per-site fix:
   retarget (to the rehomed content), reword (cite stage id as history,
   no link), or drop.
2. **Live-content disposition** — for each item above: the surviving home
   (recommend `plans/followups/` entries — e.g. `f8_e_catalog.md`,
   `f9_m1_refactor_tail.md`, divergences folded into the E-catalog file —
   or a `plans/backlog/`; pick one scheme and justify), and what merges
   into existing files (`plans/open_questions.md` absorbing Q26/Q28).
3. **Deletion manifest** — the exact `git rm -r` target plus the handful
   of files created/edited around it, and the final `plans/README.md`
   M1-row wording (shipped 2026-06-17; plans removed at P1.22, see commit
   history).
4. **Order of operations** for T1.22.99.2 so no intermediate commit state
   has dangling links (migrate → rewire → delete → grep-verify).

## T1.22.99.2 — Migrate + delete

Execute the report in its stated order; end state: folder gone, live
backlog rehomed, `grep -rn m1_core_graph_reasoning` clean repo-wide
(excluding `.git`), `plans/README.md` updated.
Gate: `./run_tests.sh` + `ruff check .` green (tests' comment edits only);
acceptance unchanged. The closeout summary goes into the commit message /
final report to the user — there is no stage file left to append to.

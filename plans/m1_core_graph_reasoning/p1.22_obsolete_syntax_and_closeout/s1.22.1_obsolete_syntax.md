# S1.22.1 — Obsolete-syntax census → purge

**Phase:** P1.22. **Priority: P0** (the language surface must match the
user's ruling before the plans that explain the history are deleted).
**Depends on:** P1.21 complete. T1.22.1.2 gated on
[`reports/c1_obsolete_syntax.md`](reports/c1_obsolete_syntax.md).

## The ruling (user, 2026-08-16)

There are **no `type` / `instance` keywords**. The kernel builds no
type-system entity-view (S1.7.23); the unified model *is* the language:
membership is `(is-a a SomeType)`, relations are declared
`(relation name T T)`, and `T` is an ordinary top atom needing **no
declaration**. The P1.7c-removed block heads `(rules …)` / `(ontology …)` /
`(facts …)` are likewise gone. Every echo of any of that must leave code,
comments, and documentation.

**Clarified by the user, 2026-08-17 — two corrections to the reading above.**

1. **`type` and `instance` are not forbidden *atoms*, only absent
   *keywords*.** Nothing stops a puzzle from using `type` as an ordinary
   relation name — `(type Subtype Type)` is well-formed the moment the
   puzzle declares `(relation type T T)`, exactly like any other relation.
   What is obsolete is the *special-cased* reading: a `(type …)` form the
   kernel interprets as a type declaration, a `(instance …)` form it reads
   as membership, and the registries/printers that served them. The census
   must therefore classify by **whether the kernel special-cases the head**,
   not by the head's spelling — an undeclared `(type …)` is an error
   because it is an undeclared relation, which is the ordinary rule.

2. **`examples/zebra.ein` is valid Ein.** The scoping sweep's premise —
   that the classic encoding is obsolete *syntax* — is withdrawn. It parses
   and loads; the ruling does not condemn it. What it does not do is solve,
   and the reasons are **ontological, not linguistic**:
   - some of the relation properties it needs are not effectively
     implemented *in that formulation* — the encoding names properties the
     rule library does not act on;
   - and it generates far too many hypotheses for the search to close.

   > "Not because its language is broken, but because problem ontology is
   > not sufficient."

   So the verdict is **rewrite, not delete**, and it is no longer a
   question the census has to answer — it is its own stage,
   [S1.22.1a](s1.22.1a_zebra_ein_modernisation.md). This stage keeps only
   the parts of `zebra.ein` that are genuinely obsolete *syntax* (block
   heads, kernel-special-cased forms) and hands the ontology work over.

Known sites from the scoping sweep (2026-08-16; census must be exhaustive):

- **`examples/zebra.ein`** — ⚠️ **re-scoped 2026-08-17, see the ruling
  above.** Not obsolete syntax and **not** to be deleted. This stage owns
  only the genuinely-removed forms if any remain there (block heads,
  kernel-special-cased `(type …)`/`(instance …)` readings) and the
  *narrative* sites that call it "the classic encoding" as though it were
  a dead dialect. Its ontology, rules and solvability are
  [S1.22.1a](s1.22.1a_zebra_ein_modernisation.md); do not sweep its
  references for removal.
- **`ein.py/src`**: `cli/saturate.py` (S1.7.23 comment + the
  `(type …)`/`(instance …)` summary print lines), `inference/compile.py`,
  `ir/to_dot.py`; block-head references in `cli/_common.py`,
  `inference/hrule.py`, `trace/answer.py`, `render/{constraints,rules}.py`,
  `ir/to_dot.py`, `kb/{__init__,render,store,from_ir}.py`.
- **`ein.py/tests`** (~20 files): `test_ir_ast.py` (incl. parametrised
  cases `"(type Person)"`, `"(instance Norwegian Nationality)"`, a rule
  matching `(instance ?a ?T)`), `test_ir_parser.py`, `test_ir_to_dot.py`,
  `inference/test_compile.py:75-78`, `inference/test_rules.py`,
  `inference/test_engine.py`, `inference/test_commitment.py`,
  `lattice/test_{contradictions,gaps}_backbone.py`, `lattice/test_p16_contract.py`,
  `kb/test_{entities,provenance,layers,render}.py`,
  `render/test_{golden_dot,slice_dot}.py`, `monotonic/test_monotonic_dumper.py`.
  (`monotonic/test_root_stability_naf.py` was already modernised inline
  on 2026-08-16.)
- **`docs/`**: grammar/examples/reserved-names pages where the classic
  forms or block heads are *taught or excused* rather than merely listed
  as removed history.

## T1.22.1.1 — Census + report → `reports/c1_obsolete_syntax.md`

Read-only. Deliver:

1. **Exhaustive site list** (`file:line`), classified: (a) real obsolete
   Ein syntax in fixtures/examples; (b) code that *emits or special-cases*
   the obsolete forms (`cli/saturate.py` printers); (c) comments/docstrings
   *referencing* removed forms; (d) docs prose; (e) **false positives to
   keep** — `Layer.ONTOLOGY`/`Layer.FACT` enum semantics, Python
   `isinstance(...)` hits, historical-stage plan files (all of `plans/m1_*`
   dies in S1.22.2 anyway — skip it), `REVIEW_M1-01.md` quotes.
2. ~~**`zebra.ein` verdict**~~ — **settled by the user, 2026-08-17: keep
   and rewrite.** The census still reports what in that file is
   kernel-special-cased syntax versus ordinary-but-undeclared relations
   (correction 1 above), because S1.22.1a needs exactly that split — but
   the delete option is off the table.
3. **Kwargs ruling** — are `:layer` / `:source` fact kwargs parser-live,
   and used anywhere outside zebra.ein? If classic-only: purge with it; if
   load-bearing elsewhere: keep, and say where.
4. **Rewrite plan per test file** — modern-syntax replacement for each
   fixture (semantics-preserving: same fact/arity shapes via `is-a` +
   declared relations), flagging any test whose *subject is the obsolete
   syntax itself* (e.g. parser cases asserting `(type Person)` parses) —
   those convert to negative tests or get deleted, per the ruling.
5. **Purge inventory** — exact file list for T1.22.1.2, with the
   suggested edit per site and the tests whose golden outputs shift
   (DOT goldens, dumper timelines).

## T1.22.1.2 — Purge

Execute the report: modernise every fixture; strip the saturate-CLI
special-case printers and every obsolete comment/docstring/doc mention;
convert-or-delete syntax-subject tests. **Not** `zebra.ein`'s ontology —
that is [S1.22.1a](s1.22.1a_zebra_ein_modernisation.md); this task touches
that file only for genuinely-removed syntax and for prose that treats it as
a dead dialect.
Gate: `./run_tests.sh` + `ruff check .` green; acceptance
verdicts/bindings unchanged; the phase-README §Acceptance-1 grep clean.
Append `## Outcome (date)` here.

# S1.22.3 — Relation-signature semantics: document the kernel/userspace split

**Phase:** P1.22, inserted after
[S1.22.1a](s1.22.1a_zebra_ein_modernisation.md) (whose `zebra.ein` rewrite
is the file the origin questions were asked against) and **before**
[S1.22.4](s1.22.4_relation_kernel_word.md) (which consumes this stage's
census as its decision input) and
[S1.22.99](s1.22.99_m1_plans_deletion.md) (the docs this stage edits are
part of the tree the deletion census inventories).
**Priority:** P1. **Estimate:** S/M (docs-only; the census below is already
done — the work is placing it). **Value:** M/H — the questions were asked
by the *project's author*, which is the definition of a docs gap; and
`docs/kernel/`'s own bar is "author a puzzle without reading engine source"
([`06_reserved_names.md` §quick reference](../../../docs/kernel/ir/03-ein-lang/06_reserved_names.md)).

**Origin — user, 2026-08-17** (root-`TODO.md` scratchpad, processed here
and pruned per the scratchpad convention; quoted in full because the block
was never committed, so this file *is* the trail):

> Look into zebra.ein and zebra2.ein.
>
> 1. Implicit type system
>
> `(relation is-a T T)`
>
> It declare relation is-a: TxT, right?
> But Ein claims it doesn't have internal type system and type relations
> has to be defined by the user.
> But what are those arguments of `relation`?
>
> `(relation right-of Attribute Attribute)` - where `Attribute Attribute`
> are used? Explicitely through rules or implicitely in the kernel?
>
> 2. Do we need `T` ? What for?
>
> Can't we just define it manually in the stdlib or even in the user code?
> User may want to have multiple orthogonal type systems?
> What if I would like to have types of relations?
>
> 3. All facts are effectively relation facts.
>
> No matter is it just manually added fact like:
> `(right-of House-2 House-1)`
>
> or it is a rule instantiation:
> `(adjacent-via next-to nation-loc Norwegian color-loc Blue)`
>
> they all define relations between different objects
> and some of objects may be relations too
> because relations are also objects, right?
>
> 4. Unary relations
>
> Even if we don't introduce unary relations we already have them:
> `(bijective color-loc)`
> `(symmetric next-to)`
>
> It's simple to draw Levi-bipartite unary relation,
> but normal graph unary relation would look like arrow starting in the
> obejct and ending without object.
>
> And important thing is that unary relations defines subsets ov objects
> (and subsets of relations also)

(Item 5 of the same block — `relation` as a stdlib word — is
[S1.22.4](s1.22.4_relation_kernel_word.md)'s origin.)

## The census — what the code actually does (2026-08-17, verified)

Every question has a definite answer **today**; none of them is answered
by `docs/kernel/`. This section is the content to be placed, with the
evidence. The executor re-verifies the line references against HEAD (later
stages may have moved them) but does not redo the investigation.

### Q1 — the signature's dual role

The kernel claim "no internal type system" **holds** (since S1.7.23), but
the signature is *not* inert — it plays two unrelated roles, and the split
is documented nowhere:

**As types — userspace only.** The loader mirrors every declaration as an
ordinary fact `(relation Name A B)` precisely so rules can match it
(`ein.py/src/ein/kb/from_ir.py:187-194`). Everything type-like keys off
that fact, in rules: std.bijection's typecheck stack
(`stdlib/bijection.ein:71` — `(relation ?R ?A ?B) ∧ (bijective ?R) ∧
(typecheck-hierarchy ?isa)`), std.algebra's converse domain/range check
(`stdlib/algebra.ein:292`), std.typing's hierarchy wiring. The kernel
never resolves a signature atom to anything —
`Relation.signature` is "opaque atoms… no `signature → Type` resolution"
(`ein.py/src/ein/kb/entities.py:74-78`).

**As structure — kernel, three signals.** The kernel reads the signature's
*shape*, never its content-as-types:

| signal | site | effect |
|---|---|---|
| signature **non-empty** | `inference/hypgen.py:251`, `inference/closed.py:84` | "declared domain relation" — eligible for hypothesis generation and for `__closed__` auto-inference; property/rule-name relations (empty signature) are skipped by both |
| signature **length = 2** | `inference/hypgen.py:293` | hypgen fills slots only for arity-2 (the M1 limit) |
| signature **atoms** | `inference/hypgen.py:537-541` | the declared type-role names (`Attribute`, `House`, `T`, …) are excluded from candidate *objects* — the enumerator never guesses about a type node |

Plus two ergonomic reads: the `:why` render template
(`kb/from_ir.py:182`) and the CLI signature display
(`cli/saturate.py:388`).

So the answer to "explicitly through rules or implicitly in the kernel?"
is **both, in different roles** — and the roles never mix.

### Q2 — `T` is convention, and why the slots need *some* atom

`T` is an ordinary, undeclared, unreserved atom
([`06_reserved_names.md` §Not reserved](../../../docs/kernel/ir/03-ein-lang/06_reserved_names.md)).
What is *not* said anywhere: the slots cannot simply be omitted, because
(a) the grammar requires ≥1 type atom (`ir/grammar.lark:97` —
`relation_decl: "(" "relation" SYMBOL SYMBOL+ kw_pair* ")"`), and (b) the
kernel keys hypothesis-eligibility on signature **presence** (Q1 table).
`T` is the conventional "don't care" filler; any atom produces the same
kernel behaviour. Orthogonal type systems already coexist in-tree —
`zebra.ein`'s split `type`/`instance` vs `zebra2.ein`'s unified `is-a` —
and types *of relations* are already legal facts
(`(is-a co-located EquivalenceRelation)` parses and stores; only a rule
gives it meaning), because of Q3.

### Q3 — relations are objects, with one qualification

Yes, and it is implemented, not aspirational: declarations are mirrored as
facts, property tags take relation names as arguments, undeclared fact
heads get auto-created `Relation` entities (`kb/entities.py:80-89`). The
one place the kernel treats a relation name as *not* an object is
`NameRef.category`: hypgen's candidate-object pool excludes non-`object`
categories (`hypgen.py:547`) — a tractability device, not a semantic
stratification. [`08_self_describing.md`](../../../docs/kernel/ir/03-ein-lang/08_self_describing.md)
already tells the userspace half of this story ("relation signatures are
data rules consume"); the kernel half is the missing link.

### Q4 — unary relations exist; the limits are hypgen and rendering

- **De facto**: every property tag is a unary fact — `(bijective
  color-loc)`, `(symmetric next-to)`, `(__closed__ R)`,
  `(type-hierarchy is-a*)`.
- **De jure**: a unary *declaration* `(relation R T1)` parses
  (`grammar.lark:97` accepts one type atom) and loads
  (`from_ir.py:164` requires name + ≥1 type). **Doc bug found**:
  [`01_grammar.md:84`](../../../docs/kernel/ir/03-ein-lang/01_grammar.md)
  claims "arity ≥ 2" — it contradicts the grammar it documents.
- **The real limits**: hypgen fills arity-2 only (`hypgen.py:293-294`), so
  a declared unary relation is never a hypothesis target; and the compact
  DOT view has no unary convention (the Levi-bipartite view needs none —
  a fact node with one incident edge just works).
- The "unary relation = subset" reading is exactly how the property tags
  already function — `(symmetric R)` carves the subset of
  relation-objects that are symmetric. Worth one paragraph in the graph
  chapter; it is the standard predicate-as-subset view and makes the
  property-tag idiom less mysterious.

## Tasks

### T1.22.3.1 — Place the census in `docs/kernel/`

Docs-only; no engine or example changes. Re-verify each `file:line` above
against HEAD first. Placement (definitive home + cross-links, per the
docs' one-home discipline):

1. **The dual-role split (Q1)** — definitive home:
   [`06_reserved_names.md`](../../../docs/kernel/ir/03-ein-lang/06_reserved_names.md)'s
   `relation` declarator row grows a subsection (or the row links to a new
   § in [`01_grammar.md`](../../../docs/kernel/ir/03-ein-lang/01_grammar.md)'s
   relation block — executor's choice, one home only): the
   userspace-types / kernel-structure table above, verbatim in spirit.
   Cross-link from
   [`08_self_describing.md`](../../../docs/kernel/ir/03-ein-lang/08_self_describing.md)
   (the userspace half lives there) and from
   [`02-data-model/01_entities.md`](../../../docs/kernel/ir/02-data-model/01_entities.md)
   (the `Relation.signature` field description).
2. **Fix `01_grammar.md:84`** — "arity ≥ 2" → matches `grammar.lark:97`
   (name + **one or more** type atoms; note the hypgen arity-2 limit is an
   *engine* M1 limit, not a grammar rule, and say where it lives).
3. **`T` (Q2)** — extend `06_reserved_names.md`'s existing "Not reserved"
   `T` bullet with the *why the slot needs an atom at all* sentence
   (grammar `SYMBOL+` + kernel presence-keying). One sentence on
   orthogonal type systems pointing at the two zebra encodings as the
   in-tree proof.
4. **Relations-are-objects (Q3)** —
   [`02-data-model/01_entities.md`](../../../docs/kernel/ir/02-data-model/01_entities.md):
   the auto-created-Relation paragraph gains the `NameRef.category`
   qualification (tractability device, not stratification).
5. **Unary relations (Q4)** — one short § in
   [`01-ein-graph/03_ein_model.md`](../../../docs/kernel/ir/01-ein-graph/03_ein_model.md)
   (the graph-node subset section already there is the natural anchor):
   property tags as the existing unary idiom, the subset reading, the
   Levi-vs-compact rendering note, and the hypgen arity-2 limit as the
   reason unary relations are facts-only today.

**Gate:** `./run_tests.sh` + `ruff check .` green (trivially — docs-only);
repo-wide grep confirms no new mention of obsolete forms (the S1.22.1
"no nostalgia" discipline applies to new prose too). Append
`## Outcome (date)` here.

## Acceptance

1. A reader of `docs/kernel/` can answer all four origin questions
   without opening `ein.py/src/` — the doc suite's own stated bar.
2. `01_grammar.md`'s arity claim matches `grammar.lark`.
3. Each fact has exactly one definitive home; other mentions link to it.
4. The origin block is pruned from the root scratchpad (done at stage
   creation — this file carries the full quote).

## Cross-links

- [S1.22.4](s1.22.4_relation_kernel_word.md) — consumes this census as
  the input to the `relation`-as-kernel-word decision.
- [`06_reserved_names.md`](../../../docs/kernel/ir/03-ein-lang/06_reserved_names.md) /
  [`01_grammar.md`](../../../docs/kernel/ir/03-ein-lang/01_grammar.md) /
  [`08_self_describing.md`](../../../docs/kernel/ir/03-ein-lang/08_self_describing.md) —
  the three ein-lang homes the placement touches.
- [S1.7.23](../p1.7_bootstrapping_zebra/s1.7.23_retire_kernel_type_system.md) —
  where the kernel type system was retired; this stage documents what was
  *left behind* (the structural reads) after that purge.
- [S1.22.1a §Outcome](s1.22.1a_zebra_ein_modernisation.md) — the
  `zebra.ein` rewrite whose `(relation type T T)` / `(relation instance
  T T)` lines prompted the questions.

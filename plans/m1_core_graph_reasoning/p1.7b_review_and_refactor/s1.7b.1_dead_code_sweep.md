# S1.7b.1 — Dead-code & stale-doc sweep

**Phase:** P1.7b (Review & refactor).
**Estimate:** ½ day. Near-zero risk — pure subtraction + comments.
**Depends on:** nothing (go first; shrinks the surface 2–5 touch).
**Findings:** [F-KER-9](findings.md#ker--inference-kernel-inferencepy-excl-monotonic),
[F-KER-8](findings.md), [F-ENG-2/3/10](findings.md#eng--monotonic-search-engine-inferencemonotonic),
[F-KB-11](findings.md#kb--data-model-kbpy), [F-RTC-7/8](findings.md#rtc--render--trace--cli--ir).

## Goal

Delete what the code no longer calls and correct what the docs no longer
describe. Every deletion in this stage was grep-verified to have **zero
functional callers** across `src/ tests/ demo/ acceptance/` (2026-06-01);
the only references are `__all__` entries and docstrings. Nothing here
changes behaviour — the success signal is "suite still green after removing
it."

## Tasks

### T1.7b.1.1 — Delete the seven dead functions (F-KER-9)

Remove each definition **and** its `__all__` entry:

| function | file:line | only refs |
|---|---|---|
| `_mode_from_query` | `inference/verdict.py:169` | a docstring |
| `filter_by_nogoods` | `inference/nogoods.py:132` | `__all__:171` |
| `has_open_hypothesis` | `inference/solution.py:56` | `__all__:78` |
| `derives_positive` | `inference/firing.py:58` | none |
| `total_filtered` | `inference/hypgen.py:90` | none |
| `reaches_hypothesis` | `inference/back_prop.py:132` | `__all__:381` + docstrings |

For `reaches_hypothesis` also fix the docstrings that claim it is "reused"
(`back_prop.py:27,33`; `commitment.py:28,191`) — `commitment.py` has its own
`_reaches_commitment`; the reuse never existed (see F-KER-10, unified in
S1.7b.3).

### T1.7b.1.2 — `Saturator` test-only surface (F-KER-8)

`Saturator.solved()` (`saturator.py:191`) returns a hardcoded `False` with a
"plugged in by P1.5/P1.7" promise that P1.7a fulfilled *elsewhere*
(`solution.is_solution_node`) — it will never be filled. **Delete `solved()`**
and any test asserting its stub. `is_stalled()` (`:155`) / `contradictions()`
(`:175`) have no `src/` callers (test-only): keep `contradictions()` only if
you want a documented convenience wrapper over `ContradictionDetector`
(`tests/inference/test_contradiction.py:259` exercises it); delete
`is_stalled()` and its obsolete "`while not solved() and not is_stalled()`"
docstring. Update/remove the corresponding tests in the same commit (allowed —
no engine behaviour changes).

### T1.7b.1.3 — Delete `verdict_entry` (F-ENG-2)

Remove `verdict_entry` (`monotonic/contract.py:157-171`), its `__all__:174`
entry, and the re-export at `monotonic/__init__.py:77,121`. It has no caller
and maps `Solution → "monotonic"` — the name of the removed `monotonic_solve`.

### T1.7b.1.4 — The unused `early_terminate` dumper hook (F-ENG-10)

The solver never calls `dumper.early_terminate(` (grep: 0). Either wire it at
the real stop sites (cleaner once S1.7b.2 gives them a single `_Step.STOP`
return) **or** delete the hook from `MonotonicDumper`/`ProgressDumper`
(`state_dump.py:299-303`) and its override in `demo/bench_monotonic.py:178`.
Recommend **delete now**, re-add in S1.7b.2 if the decomposition wants it.
Correct the "six sites … guarantees `summary`" docstring (`solver.py:54-60`)
to the five hooks actually fired.

### T1.7b.1.5 — Stale-doc corrections (F-ENG-3)

Pure comment/docstring edits — no code:

- `solver.py:62-67`: rewrite — all three entries dispatch via `entry`; drop
  "the monotonic loop raises NotImplementedError if [gaps/contradictions]".
- `solver.py:1240-1241`: delete the "Skeleton stage — S1.5b.20 — both raise
  NotImplementedError" banner above two fully-implemented functions.
- Replace every `monotonic_solve` reference (9 sites in `src/`, incl.
  `lattice.py:4`, `snapshot.py:113,119`, `config.py` docstring) with `solve`.
- `state_dump.py:3-5` and `verdict.py`: drop the `tree.state_dump` /
  `inference.tree` "mirrors" references (the tree engine was deleted) — reword
  to "engine-agnostic serialisers" / remove.

Verify with `grep -rn "monotonic_solve\|inference\.tree\|tree\.state_dump"
ein.py/src --include="*.py"` → **0** after this task.

### T1.7b.1.6 — Small dead/confusing snippets

- F-RTC-7: delete the unreachable trailing `return f"shape={GROUND_SHAPE}"`
  at `ir/to_dot.py:130` (the `isinstance(node, Atom)` arm above covers the
  last reachable case).
- F-RTC-8: `cli.py:105` → `valid = ",".join(sorted(_LAYER_BY_NAME))` (drop the
  `set(...) and dict` short-circuit obfuscation).
- F-KB-11: consolidate the four scattered "`type_names` is empty until a
  signature extractor repopulates it (S1.7.6)" notes (`pattern.py:41-46`,
  `store.py` near `:429`, `entities.py:120`) into **one** comment at the
  `Pattern.type_names` field, and have `rebuild_indexes` early-skip the
  always-empty `_rules_by_type` block (`store.py:429-441`) with that one
  pointer. Keep the seam (it is a deliberate park, not dead weight) — just
  stop it reading as a mystery.

## Acceptance

- `grep` for each of the 7 dead functions + `verdict_entry` + `monotonic_solve`
  + `inference.tree` across `src/` → **0** hits (definitions and refs gone).
- Full pytest suite green; `ruff check .` green (removing `__all__` entries
  and unused functions must not orphan an import — fix any that surface).
- No behaviour change: `bench_solve_monotonic_pypy.sh` verdict unchanged.
- One commit per task (or a single squash) so any regression is bisectable.

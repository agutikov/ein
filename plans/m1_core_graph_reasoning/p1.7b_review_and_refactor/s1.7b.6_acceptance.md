# S1.7b.6 — Acceptance: green suite, benches unchanged, metrics re-measured

**Phase:** P1.7b (Review & refactor). **The gate.**
**Estimate:** ½ day.
**Depends on:** S1.7b.1–5 (whichever shipped — this stage certifies the set).

## Goal

Prove the phase did its job *and only its job*: the code got measurably
cleaner, the two latent bugs are closed, and **no behaviour moved**. This is a
verification stage — it writes no feature code, only the metric scan + the
no-change evidence.

## Tasks

### T1.7b.6.1 — Behaviour-unchanged evidence

- Full pytest suite green: `./run_tests.sh` (604+ tests; the count only rises
  if S1.7b.3/4 added the two regression tests — note the new total).
- PyPy 3-variant acceptance green: `zebra2` → Unique (25/25),
  `zebra2-minus-15` → Ambiguity, `zebra2-bad` → Contradiction; the SAT↛⊥ /
  UNSAT↛Solution invariant holds.
- Benches byte-identical to the 2026-06-01 baseline (verdict + bindings +
  `solution_nodes`/dedup counts): `bench_solve_monotonic_pypy.sh zebra2`,
  `bench_lattice_pypy.sh`, `demo/bench_saturate.py zebra2`. Capture the
  baseline at phase start (tag it) so this diff is real.
- `ruff check .` green.

### T1.7b.6.2 — The two bug-fix regression tests

Confirm both are present and green (they fail on a pre-S1.7b checkout):

- F-KER-4: `(config :lattice-order-seed 7)` loads to
  `SolverConfig(lattice_order_seed=7)`.
- F-KB-3: a fact derived by two rules appears exactly once in
  `_facts_by_relation` / `names`.

### T1.7b.6.3 — Re-run the metric scan (the cleanliness delta)

Re-run the AST scan from the review and compare to the
[`findings.md` baseline](findings.md#metrics-baseline-2026-06-01-to-re-measure-at-s17b6):

| metric | baseline (2026-06-01) | target |
|---|---|---|
| functions > 100 ln | 9 | ≤ 2 (only with a written justification) |
| functions > ~120 ln | 6 | 0 |
| nesting depth ≥ 8 | 2 | 0 |
| nesting depth ≥ 5 | 10 | ≤ 3 |
| verified-dead functions | 7 (+1 hook) | 0 |
| `monotonic_solve` refs in src | 9 | 0 |
| `tree`-engine doc refs in src | 2 files | 0 |
| `if entry ==` in solver.py | ~24 | 0 |
| ruff | clean | clean |

Record the actual numbers in a `## What shipped` section appended to the phase
[README](README.md) (the project convention: don't delete the trail, append
the outcome — and **note honestly any finding deferred or partially done**, as
P1.7a did).

### T1.7b.6.4 — Close-out

- Append `**Status:** done — <date>` to each shipped stage file.
- Update the [P1.7b README stage table](README.md#stages) statuses.
- If any finding was consciously **not** done (e.g. the optional
  `_BaseStats`/`_TimelineMixin`, or a hot-path task reverted for regressing),
  move it to [P1.9 follow-ups](../p1.9_hypothesis_loop_followups/README.md) or
  note it as parked — don't leave it implied-done.

## Acceptance

- All of T1's evidence captured and green.
- The metric table filled with **real** post-refactor numbers showing the
  targets met (or a written reason where not).
- The phase README carries a `## What shipped` ledger with the deltas and any
  honest deviations.
- M1's own acceptance (P1.7a's gate) **still passes** — P1.7b must not have
  nudged the gate it sits behind.

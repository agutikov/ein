; S1.8.A5 — Migration: zebra.ein (and zebra2.ein) adopts
; imports for any rules promoted to the universal library.
; If Q30 lands on (b), this stage is empty.

# S1.8.A5 — Migration to imports

**Phase:** P1.8 (improvements), Theme A.
**Estimate:** ½-1 day, depending on how much zebra*.ein
content moves.
**Status:** **`std.macro` slice DONE 2026-06-04**; universal-rule promotion
PENDING (needs a stdlib *rule* module, which doesn't exist yet — A7/A12).
**Depends on:** [S1.8.A4](s1.8.a4_rule_library_location.md) (realized in A3).
**Blocks:** nothing; informs [S1.20.C](../p1.20_kernel_docs/s1.20.c_stdlib_api_reference.md)
once shipped.

> **AS BUILT (2026-06-04) — the `std.macro` slice.** Every inlined
> `(macro forall …) (macro open …)` block (10 example files + the
> `test_forall`/`test_open`/`test_stdlib_domain_elim` helpers) is replaced by
> `(import std.macro :symbols (forall open))` — the satisfying collapse the
> S1.5.9 inlining promised. The demo mirror `examples/stdlib/macro.ein` is
> **deleted** (canonical is the packaged `ein_bot/stdlib/macro.ein`); its
> anti-drift test goes with it. zebra2 golden regenerated (now leads with the
> import). Behaviour byte-identical — full suite 1128 green; the PyPy
> `--exhaustive` solve on the migrated `zebra2.ein` still certifies
> `verdict Solution, k=1, exhausted=true` with the correct grid.
>
> **Still pending (the doc's original scope):** promoting the *universal
> rules* (`symmetric` / `transitive` / `includes` / …) into an importable
> stdlib **rule** module. That needs the module to exist first (A7 `imply` /
> `converse` family, A12 relation-algebra), so it waits on those — the rule
> bodies still live inline in `zebra2.ein` for now.

## Context

After S1.8.A4 ships the library, M1's puzzles (zebra2.ein and
the branching demos) should migrate to use imports instead of
inlining the kernel rules. This stage's scope depends on the
S1.8.A4 decision:

**P1.7c note (2026-06-02).** Post-[S1.7c.4](../p1.7c_block_head_removal/s1.7c.4_migrate_and_drop_shim.md),
zebra2.ein is **already flat** — the kernel rules are top-level `(rule …)`
siblings, not nested in a `(rules …)` block. So "migrate" = delete the
promoted top-level `(rule …)` forms and add an `(import …)`; there is no
wrapper to unwrap.

- **If (a) or (c):** zebra2 → `(import "stdlib/kernel.ein")`
  for the kernel rules; keep `cross-attr-spatial-*` and other
  puzzle-specific rules inline.
- **If (b) inline-only:** this stage is empty.

## Tasks

### Task T1.8.A5.1 — Inventory zebra2.ein

What rules in zebra2.ein are universal (move to stdlib) vs
puzzle-specific (stay inline)?

- Universal: `symmetric`, `transitive`, `includes`,
  `disjunctive-prune`, possibly `cross-attr-spatial-fwd/bwd`
  if generalised.
- Puzzle-specific: condition-(N)-derived activator facts;
  zebra2-specific spatial-rule activators.

### Task T1.8.A5.2 — Migrate zebra2.ein

- **a.** Replace the universal `(rule …)` definitions with
  `(import "stdlib/…")`.
- **b.** Keep the puzzle-specific activator facts.
- **c.** Re-run `./bench_solve_monotonic_pypy.sh` — same verdict + bindings
  as pre-migration.
- **d.** Trace + node count: match pre-migration (modulo any
  trivial reordering).

### Task T1.8.A5.3 — Migrate branching demos

- **a.** Same pattern for `examples/branching/*.ein` that
  use universal rules.
- **b.** Re-run demos; node counts match pre-migration.

### Task T1.8.A5.4 — Documentation

- **a.** Update `examples/README.md` to point at
  `stdlib/README.md`.
- **b.** Each migrated demo includes a comment at top:
  `;; Imports stdlib (file: examples/stdlib/…)`.

## Acceptance

- zebra2 + demos use imports for universal rules.
- Pre/post-migration verdicts + bindings + node counts
  identical.
- Documentation updated.

## Open questions

- **Q-S1.8.A5.A — Examples as a smoke test.** Should the
  migration include a smoke-test puzzle that imports
  *everything* in stdlib to catch broken stdlib entries?
  Probably yes.

## Cross-links

- [P1.8 README — Theme A](README.md#theme-a--ein-lang-modules-imports--standard-library)
  — phase context.
- [S1.8.A4](s1.8.a4_rule_library_location.md) — prerequisite.
- [`examples/zebra2.ein`](../../../examples/zebra2.ein) — the
  primary migration target.
- [`examples/branching/`](../../../examples/branching/) —
  sibling demos.
- [S1.20.C](../p1.20_kernel_docs/s1.20.c_stdlib_api_reference.md)
  — downstream stdlib-reference doc.

; S1.8.A3 — Loader changes: resolve imports, merge modules
; into a single KnowledgeBase, detect conflicts per S1.8.A1's
; conflict policy.

# S1.8.A3 — Loader: resolve imports

**Phase:** P1.8 (improvements), Theme A.
**Estimate:** 2-3 days.
**Status:** **DONE 2026-06-04** — flatten-then-load resolution shipped per the
[A1 decision record](s1.8.a1_module_system_design.md#decision-record-2026-06-04).
**A4 (rule-library location) is realized as a side effect**: the canonical
stdlib is the package dir `ein/stdlib/` (shipped via pyproject
package-data), `std.macro` → `ein/stdlib/macro.ein`.
**Depends on:** [S1.8.A2](s1.8.a2_grammar_import_use.md);
[S1.8.A1](s1.8.a1_module_system_design.md) decisions.
**Blocks:** [S1.8.A5](s1.8.a5_migration_imports.md) (replace the inlined
`std.macro` copies with `(import …)`).

> **AS BUILT (2026-06-04).**
> - New `kb/imports.py` — `resolve_imports(forms, *, base_dir)` walks the form
>   list, replacing each `(import M …)` in place with module `M`'s
>   **recursively-resolved** forms. `std.*` → packaged `ein/stdlib/`; other
>   names → file-relative to the importer; `.ein` implied. Tiers: whole-module →
>   `_qualify` (prefix `M.` every *defined* name + reference, skipping
>   `_reserved_names`); `:as A` → prefix `A.`; `:symbols (…)` → `_select` (keep
>   the listed declarations, flat). Transitive imports re-qualify under the
>   outer namespace (D6); cycles → error via the `_loading` stack.
> - **No splice-dedup** (revised mid-build): qualified diamonds never collide
>   (re-qualification), flat-name collisions are the intended dup error, and a
>   `_done` cache wrongly starved selective re-export through two paths. Cycle
>   detection is the only cross-branch state.
> - `from_ir.load(forms, *, base_dir=None)` resolves imports **first** (one
>   import-free stream → the existing single `load()` pass — D8). `load()` is
>   import-aware transparently, so `KnowledgeBase.from_ir(parse(text))` resolves
>   `std.*`. New `KnowledgeBase.from_file(path)` sets `base_dir=path.parent`;
>   the CLI's `_load_kb_or_exit` now passes it too.
> - Tests: `tests/kb/test_imports.py` — 3 tiers vs `std.macro`, qualified-macro
>   invocation, file-relative + transitive re-export fixtures, cycle/not-found/
>   `:as`-XOR-`:symbols`/`:symbols`-not-provided diagnostics, demo-mirror
>   anti-drift. Full suite 1128 green, ruff clean.
> - **Deferred:** the `--resolve` minimized dump (D9) is dump-side, not load —
>   its own follow-up.

## Context

The grammar (S1.8.A2) produces `Import` nodes; the loader has
to actually *resolve* them: open the imported file, parse it,
recursively resolve its imports, merge symbols into the
current KB, detect conflicts.

Per S1.8.A1, the exact resolution + merge semantics depends on
the file-vs-symbol-level + namespace-scoping + conflict-policy
choices.

**P1.7c note (2026-06-02).** An imported file is itself a **flat sequence of
head-classified forms** (no block wrappers). "Merge symbols" therefore means
routing each imported form by the same classifier `from_ir.load()` uses:
`relation` → `kb.relations`, `rule` / `hrule` → `kb.rules` / `kb.hrules`,
`macro` → `kb.macros` ([S1.5.9](s1.5.9_ein_lang_macros.md)), and any plain
fact into the fact store with its **per-fact derived layer** (`_layer_of`) —
see T1.8.A3.3 + Q-S1.8.A3.A.

## Tasks

### Task T1.8.A3.1 — Path resolver

A `_resolve_import(path: str, *, importing_file: Path) → Path`
function:

- **a.** Apply the path-resolution rule from S1.8.A1.4 (repo-
  relative / file-relative / env).
- **b.** Error path: import not found, circular import.
- **c.** Cache: each path resolved once per `solve()` call.

### Task T1.8.A3.2 — Recursive loader

`load_kb_from_path(path)` becomes recursive:

- **a.** Parse the file.
- **b.** Process `Import` nodes first, before the rest of the
  top-level forms.
- **c.** For each import: resolve → load recursively → merge
  symbols.

Cycle detection: maintain a `currently_loading` set; reject
re-entry.

### Task T1.8.A3.3 — Merge semantics

Each imported form is routed by the flat head-classifier (relations / rules /
hrules / macros / facts → their respective registries), then placed per the
S1.8.A1 namespace-scoping decision:

- Flat namespace: all symbols go into the same registries.
- Module-scoped: prefix names with module path before
  insertion.
- Aliased: use the alias prefix.

Imported **facts** carry their P1.7c per-fact layer (`_layer_of`): a stdlib
module's unannotated facts derive `ONTOLOGY` — the right default for library
background. Conflict detection per S1.8.A1.3 policy.

### Task T1.8.A3.4 — Diagnostics

- **a.** Clear error messages for: import not found, circular
  import, conflict-without-override.
- **b.** Each error carries the `Loc` of the offending
  `(import …)` form.

### Task T1.8.A3.5 — Tests

- **a.** Single-import smoke: file A imports file B; symbols
  from B are visible in A's KB.
- **b.** Transitive: A → B → C.
- **c.** Circular: A → B → A. Reject.
- **d.** Conflict: A defines `foo`, imports B which also
  defines `foo`. Per the policy, error / warn / use one.
- **e.** Symbol-level filter (if (b)): only listed symbols
  arrive.

## Acceptance

- Two-file project loads end-to-end.
- Errors are clear + located.
- Existing zebra2.ein still loads (no imports — should be
  backward-compatible).
- Cross-file tests pass.

## Open questions

- **Q-S1.8.A3.A — Side effects.** If an imported file contains plain
  **facts** (not just `relation` / `rule` / `macro` declarators), do those
  facts get merged into the importing KB? Usually yes for a library; usually
  no if the imported file is conceptually a "module of rules only". Decide the
  convention — e.g. merge only the declarator forms and skip plain facts, or
  merge everything. (Post-P1.7c this is a clean head-class filter, not a
  block-presence check.)
- **Q-S1.8.A3.B — Re-export.** If A imports B and C imports
  A, does C see B's symbols? Default: yes (transitively
  visible) unless a re-export annotation says otherwise.

## Cross-links

- [P1.8 README — Theme A](README.md#theme-a--ein-lang-modules-imports--standard-library)
  — phase context.
- [S1.8.A1](s1.8.a1_module_system_design.md) — design.
- [S1.8.A2](s1.8.a2_grammar_import_use.md) — grammar.
- [`ein.py/src/ein/kb/from_ir.py`](../../../ein.py/src/ein/kb/from_ir.py)
  — the loader entry point.
- [S1.8.A4](s1.8.a4_rule_library_location.md) — downstream.

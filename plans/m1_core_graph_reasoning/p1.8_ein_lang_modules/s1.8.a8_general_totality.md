; S1.8.A8 — General totality / domain-elimination library
; form; reconcile with S1.5.8's zebra slice (which stays
; M1-blocking).

# S1.8.A8 — General totality + domain-elimination library form

**Phase:** P1.8 (improvements), Theme A.
**Estimate:** 2-3 days.
**Status:** parked; depends on [S1.5.8](../p1.5_hypothesis_loop/s1.5.8_totality_domain_elimination.md)
shipping (the zebra-scoped slice).
**Depends on:** S1.5.8 (zebra slice); [S1.8.A4](s1.8.a4_rule_library_location.md)
(stdlib location).
**Blocks:** [S1.8.A6](s1.8.a6_closure_auto_inference.md)'s
`functional ∧ total ⇒ closed` witness depends on `(total R)`
being a stable property; the library form here makes it
universally usable.

## Context

S1.5.8 ships a **zebra-scoped** `(total …)` + `domain-
elimination` + `StructuralScan`, kept M1-blocking
(2026-05-22 decision). This stage re-homes the *general* form
in the stdlib:

- Totality as a reusable library property.
- `domain-elimination` / `elimination-by-exhaustion` as a
  library rule.
- The auto-infer-totality idea (Q-S1.5.8.A) as a library
  rule that derives `(total R)` from observed bijectivity +
  count.

Concept overlap with S1.5.8 is **accepted**: S1.5.8 is the
zebra-acceptance slice, the stdlib the reusable
generalisation.

## Tasks

### Task T1.8.A8.1 — Library form of `(total R)`

- **a.** Document the property in the stdlib (S1.10.C target).
- **b.** Generalise any zebra-specific assumptions from
  S1.5.8.

### Task T1.8.A8.2 — Library form of `domain-elimination`

- **a.** The rule's pattern + assertion in the stdlib (S1.8.A4).
- **b.** `StructuralScan` opcode (S1.5.8c) is the kernel
  half; this stage owns the *user-facing rule* that consumes
  it.

### Task T1.8.A8.3 — Auto-infer-totality (Q-S1.5.8.A)

A rule that derives `(total R)` from observed bijectivity +
the domain's count predicate. Open question whether the
matcher language supports the count side; defer to S1.8.A10
(type / domain matching) if not.

### Task T1.8.A8.4 — Migration

- **a.** Where S1.5.8's zebra slice has hardcoded rules,
  point them at the library form via imports.
- **b.** Confirm zebra2 still solves uniquely + matches the
  idea-08 trace.

### Task T1.8.A8.5 — Tests

- **a.** Library form exercised on a non-zebra puzzle.
- **b.** Auto-infer-totality fires when bijectivity + counts
  align; doesn't fire when they don't.

## Acceptance

- General `(total R)` + `domain-elimination` shipped in stdlib.
- Zebra2 still works under the library form.
- Auto-infer-totality (if shipped) covered by tests.

## Open questions

- **Q-S1.8.A8.A — Auto-infer-totality on partial counts.** If
  the domain has 5 elements and the relation has 4 facts, is
  it almost-total? Suggest a different sub-type? Cross-link
  F7 §B' (instance properties → type properties).
- **Q-S1.8.A8.B — `domain-elimination` priority.** The rule's
  firing order matters for trace fidelity (idea-08 walkthrough
  expects elimination-by-exhaustion at specific points).
  Confirm M1 doesn't drift here.

## Cross-links

- [P1.8 README — Standard library § general totality](README.md#standard-library)
  — phase context.
- [S1.5.8 totality + domain elimination](../p1.5_hypothesis_loop/s1.5.8_totality_domain_elimination.md)
  — the M1-blocking zebra slice; this stage's library
  generalisation.
- [S1.8.A4](s1.8.a4_rule_library_location.md) — destination.
- [S1.8.A10](s1.8.a10_type_domain_matching.md) — composes for
  the auto-infer-totality case.
- [F7 §B' instance → type induction](../../followups/f7_rule_induction.md)
  — long-term.

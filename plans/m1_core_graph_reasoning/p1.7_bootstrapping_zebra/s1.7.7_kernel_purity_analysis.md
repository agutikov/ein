# S1.7.7 — Kernel purity analysis: `is-a` + `symmetric` + closed-world (parked)

**Phase:** P1.7 (bootstrapping) — **end-of-phase analysis stage.**
**Estimate:** unknown; research-grade. Begins with a ~½-day spike.
**Depends on:** [S1.7.6](s1.7.6_kernel_minimization.md) (the mechanical
removals; `is-a` is the last inheritance primitive once `type` /
`instance` become user-space). **`symmetric` was reclassified into this
stage by [S1.7.6 T1.7.6.4](s1.7.6_kernel_minimization.md) (2026-06-01)** —
its engine machinery proved load-bearing under an empirical deletion test.
**Status:** **parked** (user direction 2026-05-30) — Tier 3 of the
full-purity decision is an *analysis* stage, not committed execution, and
sits at the end of the phase.
**Decomposed (2026-06-01):** this stage holds the **comprehensive
inventory** (below — *every* hardcoded name the engine special-cases, not
just the S1.7.2 three) and the de-risk spike; the **per-name analysis** —
functionality, removal consequences, ein-lang-rule route, and a
name-agnostic route — is spun out one stage file per name, **S1.7.8+**
(see [§Decomposition](#decomposition--per-name-stage-files-s178)). This
mirrors how [S1.7.2](s1.7.2_dynamic_vs_hardcoded.md) spun execution out to
S1.7.6/.7: the inventory is the artefact here; each removal decision is a
child stage.
**Not an M1 acceptance gate:** zebra2 solves *with* `is-a` / `symmetric` /
`closed` as primitives, so M1-done ([S1.7.3](../p1.7a_solution_search_refactor/s1.7.3_trace_acceptance.md))
does not wait on this.

## Context

The [S1.7.2](s1.7.2_dynamic_vs_hardcoded.md) audit classified `is-a` and
`closed`/`open` as **load-bearing engine optimizations**, not mere
derivations; [S1.7.6 T1.7.6.4](s1.7.6_kernel_minimization.md) added
**`symmetric`** after an empirical deletion test:

- **`is-a`** — transitivity is already a rule, but the hardcoded part is
  hypothesis-generation **type-pruning** (`INHERITANCE_RELATIONS`,
  `hypgen.py:43,390,506`) + the inheritance views (`kb/views.py:149,169`).
  Pruning restricts candidate bindings to type-compatible instances;
  remove it and the engine generates type-incompatible hypotheses and
  leans on contradiction rules to kill them — a potential combinatorial
  blow-up.
- **`symmetric`** — the positive `(rule symmetric)` is pure user-space,
  but the *search-layer* handling is engine-hardcoded across three sites:
  hypgen emits **both orderings** as candidates (`hypgen.py` ~287); the
  on-death **mirror** writes `(not (R b a))` when `(R a b)` dies and
  **bubbles it to every ancestor kb** (`back_prop.back_propagate(promote_symmetric=True)`
  tree-path; `solver._emit_negated_fact_writeback` monotonic-path); and
  `solution.open_hypotheses` **canonicalises** `(R a b)`/`(R b a)` to one
  open entry so ambiguity `k` counts a symmetric pair **once**. Empirically
  (T1.7.6.4): deleting all three leaves zebra2 untouched (its targets are
  `*-loc` bijections — symmetry never fires) but **fails 31 tests** on
  symmetric-hypothesis puzzles. The positive rule propagates positives
  within a branch; it cannot subsume the negative + cross-branch +
  result-counting logic.
- **`closed`/`open`** — `(closed R)` underwrites NAF / domain-elimination
  **soundness** (`inference/closed.py:67,79` auto-closure;
  `hypgen.py:334`); `open` (`compile.py:244`) is the match-body
  counterpart whose exact role must be pinned first. Intersects
  [S1.7.4](s1.7.4_naf_dependency_map.md): once closure is rule-sourced,
  the static derived-NAF map becomes the soundness audit.

Removing these as rules is real work *and* risk. This stage is the
investigation that decides whether they *can* be — ending in a written
decision, possibly an **irreducible-kernel** finding (a legitimate result
worth recording, not a failure).

## Comprehensive inventory — every hardcoded name in the inference engine

[S1.7.2](s1.7.2_dynamic_vs_hardcoded.md)'s list was scoped to the
**grammar + loader**; this is the **engine** scope —
`ein.py/src/ein_bot/inference/**` plus the two KB methods the engine
calls to read a name (`store.is_symmetric`/`symmetric_relations`,
`views` `is-a`). A *hardcoded name* is any string literal the engine
branches on, looks up in `_facts_by_relation`, or holds in a name
constant. Classified by what de-hardcoding would mean:

| name | class | engine role (where) | de-naming verdict (prior) | stage |
|---|---|---|---|---|
| `is-a` | inheritance | hypgen type-pruning (`hypgen.py:45` `INHERITANCE_RELATIONS`, `:162,243,269`) + inheritance views (`kb/views.py:149,169`) | **load-bearing** (search optimisation) | [S1.7.8](s1.7.8_isa.md) |
| `symmetric` | property / engine-special | both-orderings (`hypgen.py:267,284`), death-mirror+ancestor-bubble (`back_prop.py:162,298`), open-set canonicalisation (`solution.py:40,45`), `_compute_alive` (`solver.py:325`); read via `store.is_symmetric` | **load-bearing / irreducible** (T1.7.6.4: deletion failed 31 tests) | [S1.7.9](s1.7.9_symmetric.md) |
| `closed` | property / engine-special | auto-closure `emit_closed` (`closed.py:56,68,80`) → hypgen suppression (`hypgen.py:328`) | **load-bearing** (NAF / search soundness) | [S1.7.10](s1.7.10_closed.md) |
| `open` | logical primitive (match sugar) | `_desugar_open` → `(and (absent P) (absent (not P)))` (`compile.py:314`) | **pure desugaring** | [S1.7.11](s1.7.11_open.md) |
| `not` | logical primitive | negation-as-data: stored `(not P)` facts, pair-⊥, mirror (`compile.py:269,322,462,474,502`; `contradiction.py:166`; `lookahead.py:128`; `back_prop.py:178`; `solver.py:340`; `naf_deps.py:31`) | **irreducible base** | [S1.7.12](s1.7.12_not.md) |
| `false` | logical primitive | direct-⊥ sentinel (`contradiction.py:157`; `lookahead.py:125`) | **irreducible base** | [S1.7.13](s1.7.13_false.md) |
| `and` | logical primitive | premise flatten (`compile.py:330`); nogood join (`nogoods.py:11`) | **structural sugar** | [S1.7.14](s1.7.14_and.md) |
| `or` | logical primitive | nested-only arm (`compile.py:342`); top-level lowered at load (`from_ir._match_disjuncts`, S1.7.6) | **lowered to rules** | [S1.7.15](s1.7.15_or.md) |
| `forall` | logical primitive (sugar) | `_desugar_forall` → `(absent (and G (absent B)))` (`compile.py:319`) | **pure desugaring** | [S1.7.16](s1.7.16_forall.md) |
| `absent` | logical primitive | NAF guard → `AbsentGuard` opcode (`compile.py:309`); re-eval at fire (`match`, `naf_deps`) | **irreducible base** (NAF) | [S1.7.17](s1.7.17_absent.md) |
| `eq`, `neq` | predicate registry | computed guards `Guard` (`predicates.py:76`; `compile.py:346,350`) | **registry-extensible** | [S1.7.18](s1.7.18_eq_neq.md) |
| `hypothesis`, `contradiction-under` | bookkeeping carrier heads | state-hash exclusion (`canon.py:22`); branch marking | **derivable from provenance** | [S1.7.19](s1.7.19_bookkeeping_heads.md) |
| `hypothesis-relations` | query config | restrict hypgen targets (`hypgen.py:345`) | **config key** (already data) | [S1.7.20](s1.7.20_hypothesis_relations.md) |
| `solve`, `gaps`, `contradictions` | task-class entry | A/B/C entry dispatch (`solver.py` ~69 sites; `verdict.py:54`) | **engine entry** (F3 first-classing) | [S1.7.21](s1.7.21_task_class_entries.md) |
| *internal protocol strings* | engine-internal | provenance kinds (`source`/`rule`/`hypothesis`/`rejected`), lattice node kinds (`alive`/`dead-pre`/`dead-post`/`solution`), contradiction kinds (`direct`/`pair`), heuristic modes (`lex`/`score-sum`/`most-constrained`/`popularity`/`branch-info`), NameRef categories (`object`), node labels (`dead-by-nogood`) | **not ein-lang** — enums-as-strings | [S1.7.22](s1.7.22_engine_internal_strings.md) |

Already pure (zero engine special-casing — read generically through a
property index or a rule body): `transitive`, `functional`, `total`,
`asymmetric`, `sibling-exclusive`, `square-fwd/bwd`, `adjacent-via`,
`co-located`, `domain/range-elimination`, `disjunctive-prune`,
`implies`. These need no stage: the engine never names them.

**Two tiers fall out of the table.** *Tier A — ein-lang surface names*
(`is-a` … `hypothesis-relations`, S1.7.8–S1.7.20): names that do / could
appear in a `.ein` file or a graph fact, where "express as a rule" is a
real question. *Tier B — engine control vocabulary* (S1.7.21–.22): the
task-class selector and the internal enums-as-strings; here the question
degenerates to "string → enum/dispatch table", and `solve`/`gaps`/
`contradictions` additionally connects to [F3](../../followups/f3_three_task_classes_first_class.md).

## Decomposition — per-name stage files (S1.7.8+)

Each child stage answers the user's four questions for its name(s):
**(1)** functionality connected to the name; **(2)** consequences if
removed; **(3)** how it could be implemented in ein-lang rules (or
something else); **(4)** how it could be implemented without sticking to
the name. The three original S1.7.7 targets become S1.7.8/.9/.10; the
tasks below (T1.7.7.1–.5) remain the **cross-cutting** spike + the
written conclusion that the children feed.

- [S1.7.8 — `is-a`](s1.7.8_isa.md) — inheritance / hypgen type-pruning
  (was T1.7.7.2).
- [S1.7.9 — `symmetric`](s1.7.9_symmetric.md) — search-layer machinery
  (was T1.7.7.3).
- [S1.7.10 — `closed`](s1.7.10_closed.md) — closed-world auto-closure
  (was T1.7.7.4, `open` split off).
- [S1.7.11 — `open`](s1.7.11_open.md) — third-state match sugar.
- [S1.7.12 — `not`](s1.7.12_not.md) · [S1.7.13 — `false`](s1.7.13_false.md)
  · [S1.7.14 — `and`](s1.7.14_and.md) · [S1.7.15 — `or`](s1.7.15_or.md)
  · [S1.7.16 — `forall`](s1.7.16_forall.md)
  · [S1.7.17 — `absent`](s1.7.17_absent.md) — the rule-body / ⊥ calculus.
- [S1.7.18 — `eq` / `neq`](s1.7.18_eq_neq.md) — built-in predicate registry.
- [S1.7.19 — `hypothesis` / `contradiction-under`](s1.7.19_bookkeeping_heads.md)
  — bookkeeping carrier heads.
- [S1.7.20 — `hypothesis-relations`](s1.7.20_hypothesis_relations.md) —
  query-config relation.
- [S1.7.21 — `solve` / `gaps` / `contradictions`](s1.7.21_task_class_entries.md)
  — task-class entry strings (Tier B).
- [S1.7.22 — engine-internal protocol strings](s1.7.22_engine_internal_strings.md)
  — provenance/lattice/mode enums (Tier B, survey).

## Tasks

### T1.7.7.1 — De-risk spike (the load-bearing measurement)
Neutralize `INHERITANCE_RELATIONS` type-pruning (make hypgen's
type-compatibility filter a no-op) and run the zebra2 solve gate. Record:
does it still solve? wall-time delta? hypothesis-count delta
(`MonotonicStats`)? Commit a `demo/` measurement script
(persistent-script convention). This single number gates the rest:
- still solves in budget → de-hardcoding `is-a` is viable;
- blows up → `is-a` pruning is (at least) an irreducible optimization.

### T1.7.7.2 — `is-a` design + decision
From the spike: either route the type-compatibility filter through a
**rule-sourced guard** the hypgen consults (still puzzle-authored, not
Python-hardcoded), or record `is-a` as kernel-irreducible with the spike's
numbers. If de-hardcoded: re-validate solve + wall-time + the idea-08
firing test.

### T1.7.7.3 — `symmetric` design + decision

Reclassified here by [S1.7.6 T1.7.6.4](s1.7.6_kernel_minimization.md)'s
empirical test (deleting the three search-layer sites failed 31
symmetric-hypothesis tests; zebra2 was unaffected because its targets are
bijections). The question is whether the search-layer handling can be
made rule-sourced / generation-canonical, or is irreducible. Open
questions:

- **Q1 (irreducible?)** Is the symmetric search machinery separable from
  the engine at all, or — like `is-a` pruning — an irreducible search
  optimization? The positive `(rule symmetric)` provably does not subsume
  it (it propagates positives within a branch; the machinery is negative,
  cross-branch, and result-counting).
- **Q2 (canonical generation)** Could hypgen generate **only canonical
  orderings** (`(R a b)` with `a < b` for symmetric `R`) instead of
  both-then-dedup? That would make the mirror **and** the canonicalisation
  unnecessary — symmetry handled once, at generation. Does canonical-only
  generation stay **complete** (every reachable solution still found) and
  preserve the ambiguity `k`? Wall-time delta?
- **Q3 (where does "these orderings are equivalent" live?)** Pair
  equivalence is Python-hardcoded keyed on `(symmetric R)`. Can it be a
  **declarative annotation** the search consults (rules-as-data,
  [F5](../../followups/f5_rules_as_data.md)) — e.g. a canonical-form
  function the puzzle supplies — or is that just relocating the hardcode?
- **Q4 (ancestor-bubbling)** Is the cross-branch death-bubbling
  fundamentally symmetry-aware, or the *general* nogood-bubbling mechanism
  that merely also carries the mirror fact? If general, only the mirror
  fact is symmetric-specific and Q2/Q3 are the whole question.
- **Q5 (gate blind spot)** zebra2 (the M1 gate) does **not** exercise
  symmetric hypotheses (bijection targets). Should a symmetric-hypothesis
  solve (a `co-located` / `next-to` puzzle) become a **standing regression
  fixture** so future kernel edits don't silently break the symmetric path
  the gate can't see? (T1.7.6.4's deletion was green on zebra2.)

Decision: either a **canonical-generation redesign** (re-validate the 31
tests + the symmetric-hypothesis fixtures + wall-time), or record
`symmetric` as kernel-irreducible search-layer logic with the T1.7.6.4
numbers.

### T1.7.7.4 — `closed`/`open` design + decision
Pin `open`'s role (`compile.py:244`); study whether closed-world soundness
can rest on rule-sourced closure given the `absent` primitive + the
[S1.7.4](s1.7.4_naf_dependency_map.md) derived-NAF map. Decide:
de-hardcode (re-validate the gaps / domain-elimination fixtures) or record
irreducible.

### T1.7.7.5 — Written conclusion
Update `docs/kernel/ir/01-ein-graph/03_ein_model.md` §6 with the final
kernel boundary: either minimal `{relation, rule, hrule}` + logical
primitives, or that boundary **plus** the irreducible inheritance /
**symmetry** / closure primitives, with the evidence.

## Acceptance

- The spike's numbers are recorded.
- A written decision per primitive (`is-a`, `symmetric`, `closed`/`open`):
  de-hardcoded (zebra2 still solves; cost recorded) **or**
  irreducible-kernel (with evidence).
- §6 reserved-names doc reflects the final boundary.

## Connections

- [S1.7.6](s1.7.6_kernel_minimization.md) — the tractable removals this
  follows; its **T1.7.6.4 empirical test** is what moved `symmetric` into
  this stage (deletion failed 31 symmetric-hypothesis tests).
- [S1.7.4](s1.7.4_naf_dependency_map.md) — the derived-NAF map is the
  soundness instrument for the `closed` removal.
- [F1 categorical](../../followups/f1_categorical_formulation.md) /
  [F5 rules-as-data](../../followups/f5_rules_as_data.md) — the
  irreducible-kernel question is exactly "what is the minimal calculus the
  rest is data over".

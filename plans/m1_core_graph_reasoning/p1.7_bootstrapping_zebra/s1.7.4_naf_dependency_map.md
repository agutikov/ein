; S1.7.4 — Static NAF dependency map. A defensive load-time
; warning that surfaces which AbsentGuards depend on derived
; relations — the rules whose correctness leans on the fire-time
; re-eval shipped in S1.5a.1 T1.5a.1.1. Pure observability;
; doesn't change engine behaviour.

# S1.7.4 — Static NAF dependency map (observability)

**Phase:** P1.7 (bootstrapping: Zebra puzzle end-to-end).
**Estimate:** ~half a day; compile-plan walk + load-time warning.
**Status:** parked; spun out of [S1.5a.1](../p1.5a_zebra_solution/s1.5a.1_naf_semantic_rearch.md)
2026-05-24 because the original T1.5a.1.2 is pure observability,
independent of the engine fix that already shipped. Promote when
a derived-NAF rule lands with surprising behaviour and the
author wants to know upfront which derivations their NAF leans
on.
**Depends on:** [S1.5a.1](../p1.5a_zebra_solution/s1.5a.1_naf_semantic_rearch.md) shipped
(`match.absents_still_pass` is the runtime guarantee this stage
documents statically).
**Blocks:** nothing.

**Re-scoped note (2026-05-30):** the full-purity pass parks the
`closed`-as-rule question in
[S1.7.7](s1.7.7_kernel_purity_analysis.md) (the load-bearing
analysis). If `closed` is ever de-hardcoded, NAF soundness rests
entirely on rule-sourced closure — making *this* static
derived-NAF map the soundness instrument. Refresh the expected
rule list in T1.7.4.3.b to current zebra2 names (`adjacent-via-*`,
`disjunctive-prune-*`, the `is-a*` typechecks).

## Context

S1.5a.1 T1.5a.1.1 closes the NAF race at runtime — every
`AbsentGuard` is re-evaluated at fire time, so a rule whose NAF
references a derived relation is *correct*, full stop. What the
engine doesn't tell the author is **which** of their rules rely
on that re-eval. A rule with `(absent (R …))` where R is a
declared-only relation (only `(facts)` / `(ontology)` entries)
behaves identically under enqueue-time and fire-time semantics;
a rule where R is derived by another rule is only sound because
of the re-eval. The distinction matters for trace explanations
and for tuning priorities.

## Tasks

### Task T1.7.4.1 — Compile-plan NAF→relation walk

- **a.** Walk every compiled `JoinPlan` in `Engine.cache`; for
  each top-level (or nested) `AbsentGuard.sub_steps`, collect
  the relation names every `Scan`/`Join` step inside it
  references. Emit `(rule_name, activator_args, naf_relations)`
  records.
- **b.** Reuse the [`closed.producible_relations`](../../../ein.py/src/ein_bot/inference/closed.py)
  test ("some rule positively asserts R") to classify each NAF
  relation as **derived** vs **declared-only**.
- **c.** Surface the map programmatically:
  `Engine.naf_dependency_map() -> list[NafDep]` or similar. The
  return shape should be diff-friendly for golden tests.

### Task T1.7.4.2 — Load-time warning

- **a.** After `Engine.compile_all()` (or at the first
  `Saturator.saturate()` call — whichever is the natural single
  invocation site), emit a `warnings.warn(...)` for every rule
  with at least one derived-NAF dependency. The message names
  the rule, the activator (if any), and the derived relations.
- **b.** Configurable via a `SolverConfig` flag
  (`warn_derived_naf: bool = True`); defaults on but silenceable
  for tests that intentionally exercise the case.
- **c.** Documented in
  [`docs/kernel/inference/README.md`](../../../docs/kernel/inference/README.md#naf-semantics--fire-time-re-evaluation-s15a1)
  as a paragraph under the existing NAF subsection.

### Task T1.7.4.3 — Tests

- **a.** Unit: rule with declared-only-NAF → no warning. Rule
  with derived-NAF → exactly one warning naming the right
  relation(s).
- **b.** Smoke: load `examples/zebra2.ein` and assert the
  warnings list matches the expected derived-NAF rules
  (`cross-attr-spatial-fwd`, `cross-attr-spatial-bwd`,
  `disjunctive-prune`, `typecheck-arg-0`, `typecheck-arg-1` —
  the last two NAF on `is-a*` which is derived from `is-a` by
  `(includes is-a is-a*)` + `(transitive is-a*)`).

## Acceptance

- `Engine.naf_dependency_map()` is callable and returns the
  static map.
- Loading zebra2 (and any other example with derived-NAF rules)
  emits one warning per derived-NAF rule when
  `warn_derived_naf=True`.
- pytest covers both the positive and negative cases per
  T1.7.4.3.

## Open questions

- **Q-S1.7.4.A — Warning vs lint vs no-op.** Three escalation
  levels: silent map, `warnings.warn`, or a hard error if the
  author forgot to assign a high-enough priority to the
  deriving rule. Default is the middle one because the engine
  fix makes derived-NAF *sound*; a hard error would block
  perfectly-correct rule sets.
- **Q-S1.7.4.B — Nested NAF handling.** A `(forall ?b (G) B)`
  desugars to nested AbsentGuards; the dependency walk should
  enter both levels. Decide at implementation time whether to
  dedupe relations seen at multiple nesting depths.

## Cross-links

- [S1.5a.1](../p1.5a_zebra_solution/s1.5a.1_naf_semantic_rearch.md) — the runtime fix
  this stage documents statically.
- [`inference/closed.py`](../../../ein.py/src/ein_bot/inference/closed.py)
  — `producible_relations` is the building block for the
  derived-vs-declared classification.
- [`docs/kernel/inference/README.md`](../../../docs/kernel/inference/README.md#naf-semantics--fire-time-re-evaluation-s15a1)
  — the prose home for the documentation half.

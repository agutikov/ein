; S1.9.E19 — Unsat-core minimisation. Shrink the unsat-core to
; a minimal subset before storing on the dead leaf — purely for
; trace readability.

# S1.9.E19 — Unsat-core minimisation

**Phase:** P1.9, §engineering / UX.
**Estimate:** M (1-3 days).
**Value:** L (UX only).
**Status:** ✅ **DONE 2026-06-15** — shipped in `inference/min_core.py`
(provenance-based). The re-saturation deletion-MUS the Tasks below specify is
**NAF-unsound** and was *not* shipped. See [§ Implemented](#implemented-2026-06-15).
**Renamed 2026-08-16** → `frontier.py::smallest_contradiction_frontier` —
see [§ Renamed](#renamed-2026-08-16--r3).
**Catalog row:** [P1.9 README — Engineering / UX](README.md#engineering--ux)
E19.

## Renamed (2026-08-16 — R3)

REVIEW_M1-01 §3 confirmed the name over-promised:
"minimal" is true only over the derivations provenance happened to *record*
(one justification per fact, first derivation wins at the
`store.add_and_index_fact` dedup seam), so the result can flip on rule
`:priority` alone — see R3 §E3.
Renamed per R3: `min_core.py`'s "minimal-unsat-core" function →
[`frontier.py::smallest_contradiction_frontier`](../../../ein.py/src/ein/inference/frontier.py)
(smallest *recorded* single-witness source frontier; **not** a subset-minimal
MUS), tests → `tests/inference/test_frontier.py` (+ an order-dependence pin).
The sections below are the pre-rename historical record.
The recorded-derivations caveat was **lifted 2026-08-17** by
S1.21.7
(P1.21 — moved out of this catalog the same day): re-derivations are recorded
as alternative justifications and the frontier is an AND/OR search, so the
result no longer moves with rule-firing order. That stage also shipped the
use-site below ("the contradictions answer calls it"), unwired since 2026-06 —
`zebra2-bad`'s reported core went 38 facts → 1.

## Implemented (2026-06-15)

Shipped as `inference/min_core.py` (since R3:
`frontier.py::smallest_contradiction_frontier(kb)`) — but **not** the
re-saturation deletion-MUS the Tasks below describe. That method (drop a core
fact, re-saturate, keep it dropped if still ⊥) is **unsound under this engine's
NAF**: removing a fact can flip an `(absent P)` premise and *fabricate* a
contradiction the full KB never had, so "the subset still contradicts" ≠ "the
subset reproduces the real contradiction." Measured on `zebra2-bad`, that
minimiser degenerates to a structural-declaration residue (relation/property
decls whose removal merely breaks saturation), not the conflict — the same NAF
wall as E7's clause generalisation and
E13.

The sound deliverable, built on E6's
`walk_premises`: `KnowledgeBase.unsat_core(witnesses)` unions the source frontier
of *every* contradiction witness, which over-states a single propagating cause.
The shipped function instead returns the **smallest single-witness frontier** —
one *real* derivation's leaves, so it is sound by construction, NAF-safe, and
needs no saturation (pure provenance). On `zebra2-bad`: one injected fact → 123
witnesses → a 38-fact union, but the smallest single-witness frontier is the
**1-fact injected culprit** `(color-loc Green House-1)` — the tightest possible
trace explanation. `tests/inference/test_min_core.py` (now
`test_frontier.py`) pins it.

**Use site:** ✅ **wired 2026-08-17** by
S1.21.7's
companion step (unwired 2026-06 → 2026-08; R3 §E4 found zero production
callers). `monotonic/_state._source_frontier_core` and `commitment.py`'s
`dead-pre` / `dead-post` cores now call
`smallest_contradiction_frontier(kb, witnesses)`, so the `k = 0` verdict
carries the legible "why" rather than the union: `zebra2-bad` reports 1 fact
(the injected culprit) instead of 38. `_union_dead_cores` still unions across
*dead commitments* — no single dead explains an exhausted-lattice unsat — but
over far smaller per-dead cores.

## Context (original spec — its re-saturation method is NAF-unsound; see § Implemented)

When a contradiction fires, the unsat-core (premises that
produced it) may be larger than the *minimal* set that
implies the contradiction. Shrinking it (delta-debug or
linear-time minimisation) improves trace readability:
"these N facts together contradict" vs "these M ≫ N facts
include the N that matter".

Pure UX (no correctness gain), but composes with E7
(learned-clause from unsat-core).

## Tasks

### Task T1.9.E19.1 — Minimiser

A `minimise_core(core, kb) → set[Fact]` helper. Strategy:
remove each fact one at a time, re-run the contradiction
check; if still contradicts, keep removed. Greedy.

### Task T1.9.E19.2 — Use sites

Apply at contradiction-record time. Cached so re-derivation
isn't needed.

### Task T1.9.E19.3 — Tests

Synthetic puzzle with a large redundant core → minimiser
shrinks correctly.

## Acceptance

- Minimiser ships; cores in traces are visibly smaller.
- Composes with E7.

## References

Delta-debugging; SAT unsat-core minimisation.

## Cross-links

- [P1.9 README — E19](README.md#engineering--ux).
- E6 transitive premise walk
  — the walker this stage consumes.
- E7 learned-clause —
  consumer of the minimised cores.
- P1.6 trace renderer.

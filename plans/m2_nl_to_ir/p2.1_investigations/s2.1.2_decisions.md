# S2.1.2 to S2.1.6 — Decision documents

**Phase:** P2.1 (investigations).
**Estimate:** ~6 days combined.
**Depends on:** S2.1.1.

> Each of S2.1.2–S2.1.6 follows the same shape: a one-page decision
> document under `docs/decisions/`. Bundled here to keep them
> visible as a set — they are tightly coupled.

## S2.1.2 — Ambiguous parses

**Resolves:** [Q8](../../open_questions.md#q8--ambiguous-parses).

Working answer (parse-level hypotheses on the IR). Document:

- justification — point at the [idea 04](../../../docs/ideas/04-nlp-to-graph-to-solver-pipeline.md#multiple-variant-complication)
  position;
- shape of the IR wrapper — `(hypothesis-parse :id <id> :sentence <text> :alternatives (...))`;
- engine impact — the M1 hypothesis driver (P1.5) handles them
  automatically.

Tasks:

- T2.1.2.1 — write `docs/decisions/M2-ambiguous-parses.md`.
- T2.1.2.2 — add the new IR wrapper to the spec under
  `docs/ir.md`; reserve the keyword.
- T2.1.2.3 — mark Q8 resolved.

## S2.1.3 — Ontology provenance

**Resolves:** [Q9](../../open_questions.md#q9--ontology-provenance).

Working answer (library + override). Document:

- the three demo puzzles' ontology files committed under
  `examples/ontologies/{zebra,einstein,grid-1}.ein`;
- override mechanism — CLI `--ontology=...` and an LLM-extracted
  ontology surfaced to the user for review.

Tasks:

- T2.1.3.1 — write `docs/decisions/M2-ontology-provenance.md`.
- T2.1.3.2 — stub the three ontology files (content lands in P2.4).
- T2.1.3.3 — mark Q9 resolved.

## S2.1.4 — LLM as surface generator?

**Resolves:** [Q7](../../open_questions.md#q7--llm-as-surface-generator).

Working answer (templates for the proof; LLM optional for the final
narration). Document the architecture as in
[idea 08 §Probably the right architecture is](../../../docs/ideas/08-human-style-deductive-trace.md#why-this-is-a-hard-problem)
— graph engine → trace planner → optional LLM surface generator.

Tasks:

- T2.1.4.1 — write `docs/decisions/M2-surface-llm.md`.
- T2.1.4.2 — sketch the surface-generator prompt template (with a
  *no-content-modification* invariant — the prompt cannot add or
  remove rule firings).
- T2.1.4.3 — mark Q7 resolved.

## S2.1.5 — Direct LLM → SMT acceptable?

**Resolves:** [Q10](../../open_questions.md#q10--direct-llm--constraint).

Working answer (never default; debugging-only via `--no-ir` flag
that emits SMT-LIB directly and cross-checks the M3 engine output).

Tasks:

- T2.1.5.1 — write `docs/decisions/M2-direct-llm-smt.md`.
- T2.1.5.2 — mark Q10 resolved.
- T2.1.5.3 — note: cross-checking implementation deferred to M3.

## S2.1.6 — Model + GBNF strategy

**Resolves:** [Q23](../../open_questions.md#q23--local-model-choice),
[Q24](../../open_questions.md#q24--one-gbnf-or-many).

Working answer (Qwen3-30B-Instruct primary + Phi-4 14B fallback;
many task-specific GBNFs). Document:

- benchmark results from a small fixed prompt across the candidate
  models;
- model file SHA-256 + tokeniser fingerprint pinned for
  reproducibility (acva pattern);
- GBNF inventory — one per task (ontology / facts / definite
  descriptions / ambiguity-flag) committed to
  `grammars/`.

Tasks:

- T2.1.6.1 — write `docs/decisions/M2-model-gbnf.md`.
- T2.1.6.2 — populate `grammars/README.md` with the inventory.
- T2.1.6.3 — mark Q23, Q24 resolved.

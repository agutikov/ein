# Python
*.pyc
__pycache__/
*.egg-info/
*.egg

# Test / lint caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Build / dist
build/
dist/

# Virtualenvs
.venv/
venv/
.venv*
# Engine dump trees (`--dump-states DIR`). **Anchored** to the repo root: the
# bare `dump` / `dumps` these replace matched any path component, and silently
# excluded `ein.rs/crates/ein-render/src/dump/` — a source module, ignored
# because it shares a name with a scratch directory.
/dump/
/dumps/

# Machine-specific bench artifact (the tables in docs/kernel/inference/
# features.md are the committed record; regenerate with utils/feature_matrix.py)
utils/feature_matrix_results.json

# ── M1a (the Rust port + the parity harness) ──────────────────────
# Cargo's build tree — and the second one, for the `fork-delta` build of
# S1a.6.9 (a feature build kept in its own target dir so it does not thrash
# the shipping one; `utils/fork_delta_verify.py` reads its binary).
ein.rs/target/
ein.rs/target-fd/
# Harness artefacts: every run wipes and rewrites this.
conformance/out/
# A fuzz session's crash-parity candidates — inputs where ein.py raises and
# the two engines agree on the class and the exit code. They are corpus
# *candidates*, one session's worth at a time, not findings; a checked-in one
# is a `crash-parity` entry with a name.
conformance/fuzz_findings/crash-parity-candidates.txt
# Bench and profile artefacts (S1a.6.1): raw JSON + perf data, machine-specific
# and per-run. The committed record is the tables in
# plans/m1a_rust/p1a.6_performance/baseline.md — the same split
# utils/feature_matrix_results.json uses. NOT wiped between runs: the next
# re-measure wants a diff, not a fresh start.
ein.rs/bench-out/
# The stdlib's build-time copy. The checked-in one is repo-root `stdlib/`;
# `ein.py/_build.py` writes this one during `build_py`, so a wheel is
# self-contained without there being two stdlibs to keep in step
# (plans/m1a_rust/design/11_shared_assets.md).
ein.py/src/ein/stdlib/
ein.rs/target-*
